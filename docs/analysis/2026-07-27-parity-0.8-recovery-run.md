---
summary: "Run record CoS della recovery UI MediFlow 0.8, con ledger Claude, contratti, prove e blocker di promozione."
read_when:
  - "Valutando il candidato locale UI/parity 0.8."
  - "Ricostruendo branch, ownership, prove runtime o blocker residui del lavoro Claude."
---

# Recovery UI e parity MediFlow 0.8

Stato documento: `SECONDARY / RUN RECORD`

Run ID: `MFP-0.8-COS-20260727-01`

Data: 2026-07-27/28

Modalità: `manager-goal`, controller `CoS parity 0.8`

Verdetto: `PARTIAL / HOLD_PROMOTION`

Questo dossier registra il candidato locale e le prove eseguite. Non sostituisce
gli ADR, la matrice parity canonica o i runbook nativi.

## 1. Contratto del run

| Campo | Valore |
| --- | --- |
| Root controller | `/Users/leonardopegollo/.codex/worktrees/5134/medical-record-app` |
| Root candidata | `/Users/leonardopegollo/.codex/worktrees/mediflow-0.8-release-candidate-local/medical-record-app` |
| Branch candidata | `codex/mediflow-0.8-release-candidate-local` |
| Base runtime prima del packet parity | `9ee798887fbba93b50d644f223a9a14cd85fa71c` |
| Tree della base runtime | `e4e5b69c7d034822dc7a311181e9dc08336e4ade` |
| Controller | GPT-5.6 Sol high |
| Lane | Terra high per packet, verifica e review lean; CoS Sol high per integrazione e verdetto |
| Dati | Solo fixture e database sintetici |
| Esclusioni | Database reale, PHI/PII, push, PR, merge, tag e release |
| Regola di promozione | Tutti i gate richiesti devono essere terminali sullo stesso candidato |

La base identifica il codice prima del packet documentale corrente. Il commit
finale del packet non viene usato come prova auto-referenziale del runtime.

Il tentativo storico di ricostruzione da screenshot è escluso per istruzione
del committente. Lo scaffold intelligente resta un filone separato.

## 2. Fonti recuperate

| Fonte | Identità | Esito |
| --- | --- | --- |
| Task Codex storico | `019f943d-092b-7af0-9b02-239b4bb67624` | Usato come checkpoint storico, non come stato corrente |
| Task Codex storico | `019f99d3-d39d-7ea1-8a88-06b06bf1e6c5` | Usato come checkpoint storico, non come stato corrente |
| Sessione Claude corrente | `60b945...jsonl` | SHA-256 `4187a077...`; recovery UI e test |
| Sessione Claude corrente | `e2f387...jsonl` | SHA-256 `cc18e901...`; handover e rettifiche |
| Handover repository | `docs/analysis/2026-07-26-handover-interfaccia-apple.md` | Utile, ma precedente al commit Claude finale |
| Branch Claude corrente | `apple/ui-harmonization-2026-07-26` | Fonte UI corrente, pulita |

Il commit Claude corrente è `6920045f1fc75ce9ed7cf8a81785163041bdbf12`.
Il commit ha tree `2706b8ad8aa1bcbca1240dd8a29cb77601f36b72`.

## 3. Ledger recovery e worktree

| Ruolo | Path | Branch / HEAD | Stato e ownership |
| --- | --- | --- | --- |
| Coordinamento | `.codex/worktrees/5134/medical-record-app` | detached `4fbc8ee74d548949166f76d67fb1d419f48230c0` | Pulito; nessuna implementazione |
| Claude UI corrente | `.codex/worktrees/404d/medical-record-app` | `apple/ui-harmonization-2026-07-26` / `6920045f...` | Pulito; fonte UI |
| Candidato finale | `.codex/worktrees/mediflow-0.8-release-candidate-local/medical-record-app` | `codex/mediflow-0.8-release-candidate-local` / base runtime `9ee798887...` | Integrazione controllata del CoS |
| Backend 0.8 | `.codex/worktrees/mediflow-0.8-integration-2/medical-record-app` | sorgente `8083bc05...` | Integrato con i fix del gate audit |
| Packet web 400% | `.codex/worktrees/mediflow-0.8-web-zoom-combined` | `codex/mediflow-0.8-web-zoom-combined` / `7243b8ec...` | Verifier PASS; integrato |
| Packet universal app | `.codex/worktrees/mediflow-0.8-mobile-runtime` | `codex/mediflow-0.8-mobile-runtime` / `e940dc46...` | Verifier PASS; integrato |
| Packet macOS clipping | `.codex/worktrees/mediflow-0.8-macos-clipping` | sorgente `6b911637...` | Codice integrato e verificato sul bundle già costruito |
| `main` locale | `Antigravity/medical-record-app` | `main` / `2876c583...` | Pulito, ma non è la candidata |
| Apple 144 | `.codex/worktrees/144-macos-patient-split/medical-record-app` | `codex/144-macos-patient-split` / `3fd1e7a3...` | Tre file tracciati dirty; ownership esterna |
| Baseline Opus | `.codex/worktrees/apple-mobile-ui-opus5/medical-record-app` | `codex/apple-mobile-ui-opus5` / `ab4909fd...` | Directory build non tracciata; non usare |
| Scaffold | `.codex/worktrees/wul499-openminis-audit/medical-record-app` | `codex/WUL-499-intelligent-scaffold-adr` / `4aa6873d...` | Separato e fuori packet |

La fingerprint del diff dirty Apple 144 è
`5d6120ce5fe8a529a5dfcb74c3bc12f14f722732831465b090d23f3138424acb`.
Il controller non ha modificato quel worktree.

Il ramo Claude contiene 20 commit e `+6709/-616` rispetto ad `ab4909fd`.
Contiene 30 commit e `+8183/-715` rispetto alla base comune `4fbc8ee74`.

Il backend e la UI si sovrapponevano solo su due file:

- `components/kree8/areas/turno-area.tsx`
- `components/kree8/kree8-clinical-cockpit-shell.module.css`

Le modifiche erano su hunk distinti. Il backend è ora integrato nella base
`9ee798887...`, insieme ai fix del gate audit.

## 4. Packet consolidati

| Packet | Commit sorgente | Commit nella candidata | Ownership |
| --- | --- | --- | --- |
| Contratti prescrizioni Apple | `9f0914bc82b08b56d32eed4d28a824eed53ad916` | `b9d8b5828e1a82ba703a2730ac823560a6843f61` | Cinque file Swift e test |
| Harness parity web | `5f9f9b9dd94af1e53df9c73fea97faae63f68a19` | `a651824c596b642046046e764e46a3fa02967b4a` | Sei file E2E |
| Reflow web 400% | `130262c35...` + `fb97ce8d0...` + `c237e5d5...` | `b5604aba0...` + `b29b8f6eb...` + `1194623ab...` | Due CSS, scrollspy e test |
| Accessibilità universal app | `e940dc4654d8e4abb95bc4dd846b69fe84530482` | `3c38da5c67c72a6136f5dfd8af5e6baf1706179d` | Cinque file Swift/XCUI |
| Backend e audit | `8083bc05...` e follow-up | `273ce2b38...`, `aedb3f1f7...`, `aea29721e...` | Backend integrato; flusso e delegati audit validati |
| Evidence Stack web | `0d247482101f2901db1d7b5351223d7abe3b2487` | stesso commit | Stati popolato, curation, empty, loading ed error |
| Clipping macOS | `6b911637...` | `5f3b79198...` | Limiti sidebar e geometrie integrati |
| Probe AX macOS | finding storico e hardening | `8cb406868...` + `9ee798887...` | Selezione riga nativa, contenuto patient-bound e protezione dei processi esterni |

La base `9ee798887...` include backend, web, codice macOS e hardening del probe. Il blocco Xcode
corrente riguarda la ripetizione della build, non l'integrazione del codice.

### Correzione P0 chiusa

I default nativi usavano valori non accettati dal server:

- servizio: categoria `specialistica` e priorità `p`;
- protesica: categoria `ausilio` e stato `ordered`.

Il packet usa ora i valori wire canonici. I default sono `visit/routine` per
il servizio e `prescribed/standard` per la protesica.

## 5. Contratto di design osservato

Non esistono `PRODUCT.md` o `DESIGN.md` alla radice. Il sistema è documentato
da ADR 0078 e dai corpus Lume e Vetro Clinico.

| Tema | Contratto osservato |
| --- | --- |
| Direzione | Lume è la lingua di destinazione; Vetro Clinico è transitorio |
| Struttura | Modello focale con fuoco, penombra e buio operativo |
| Materiali | Contenuto clinico opaco; vetro solo nel chrome di sistema |
| Tipografia | Voce per prosa; Registro per codici, date, dosi e valori |
| Colore | Solo semantica clinica, stato e azioni prominenti |
| Continuità | Il Filo collega solo elementi realmente connessi |
| Apple | Controlli, sidebar, toolbar, menu, sheet e focus restano nativi |
| Web | Struttura web nativa, stesso contenuto e stessi stati |
| Accessibilità | WCAG 2.2 AA sul web; equivalente di piattaforma sul nativo |

Il candidato preserva questo sistema. Il run non apre un redesign.

## 6. Audit qualità del codice Claude

| Priorità | Finding | Evidenza | Stato |
| --- | --- | --- | --- |
| P0 | Default Apple incompatibili con il contratto wire | Enum e payload Swift contro validatori web | Corretto |
| P1 | Modello workspace monolitico | `PairedPatientsWorkspaceModel.swift`: 4.497 righe | Aperto |
| P1 | Client home-base molto accoppiato | `HomeBasePatientsClient.swift`: 1.468 righe | Aperto |
| P1 | Viste condivise grandi | Workspace 1.185 righe; worklist 936 righe | Aperto |
| P1 | Errori best-effort poco visibili | Fetch ambulatori usa `try?` e conserva il valore precedente | Aperto, non bloccante |
| P1 | Tecnologie assistive non terminali | VoiceOver macOS reale PASS; VoiceOver iPhone/iPad non provato; screen reader web non terminale | Bloccante per promozione |
| P1 | Accessibilità automatica iOS/iPadOS | Audit XCTest e matrice AX5 sul candidato | Chiuso, non equivale a VoiceOver |
| P1 | Probe AX non rappresentava il click reale | Il probe premeva il testo statico e non legava il corpo al paziente | Corretto; typecheck e 6/6 test PASS |
| P1 | ADR 0087 contraddiceva schema e backup | L'ADR dichiarava assente una foundation già presente in migrazione, bootstrap, backup e purge | Corretto: `Accepted`; writer, route, UI e apply restano assenti |
| P1 | Intestazioni sidebar tagliate | Codice clipping integrato; runtime reale a 1100/1300/1600 PASS | Chiuso sul bundle costruito |
| P1 | Nuova build Xcode non ripetibile | `disk5s1` non montabile; `fsck_apfs` `-69845` | Bloccante infrastrutturale |
| P1 | Stack documentale web | Evidence Stack E2E 2/2 con curation e stati distinti | Chiuso sul web |
| P1 | Matrice canonica datata | Riconciliazione nel packet corrente | Corretta |
| P2 | Scrollspy web legge geometrie su ogni scroll | Listener capture globale, cleanup corretto | Follow-up lean, non bloccante |
| P2 | Default form duplicati | Modello workspace 4.501 righe e reset separati | Debito preesistente |
| P2 | Helper E2E focus permissivo | Verifica uscita dall'origine, non il target semantico atteso | Follow-up test |

La presunta duplicazione di controlli paziente non è provata. Il finding resta
escluso finché una revisione statica o runtime lo dimostra.

## 7. Matrice capability verificata

| Capability | localhost | iPhone | iPad | macOS |
| --- | --- | --- | --- | --- |
| Agenda | PASS produzione, empty state e filtri | PASS audit e navigazione sintetica | PASS audit e navigazione sintetica | PASS click-map, focus e VoiceOver reale |
| Pazienti | PASS, lista e stato vuoto | PASS worklist, AX5 e operabilità | PASS list-detail, resize/orientamento e AX5 | PASS click-map e selezione AX su fixture sintetica |
| Diario globale | PASS, stato vuoto e timeline | PASS apertura superficie | PASS apertura superficie | Superficie presente; capability host non caricata |
| Scheda paziente | PASS produzione con DB sintetico | PASS fixture sintetica | PASS fixture sintetica | PASS click reale e selezione AX |
| Prescrizioni servizio/protesica | Contratto web canonico | 15 test wire verdi | 15 test wire verdi | 15 test wire verdi |
| Condivisione FHIR | 4 casi E2E verdi | Codice capability-gated | Codice capability-gated | Controllo presente, disabilitato nella fixture |
| Stack documentale | Evidence Stack E2E 2/2: popolato, curation, empty, loading, error | Pannello Archivio Intelligente PASS | Pannello Archivio Intelligente PASS | Lettura nativa; curation host per ADR 0076 |
| Scaffold intelligente | Fuori packet | Fuori packet | Fuori packet | Fuori packet |

## 8. Control-to-action map

La mappa copre i controlli core verificati. I controlli non elencati restano
non certificati e bloccano una dichiarazione di parity completa.

| Superficie | Controllo | Identifier | Azione / servizio | Mutazione e stati |
| --- | --- | --- | --- | --- |
| Web | Agenda | nome AX `Agenda` | Cambia area cockpit | Nessuna write; loading/empty/error |
| Web | Pazienti | nome AX `Pazienti N` | Cambia area e carica worklist | Selezione locale; empty/error |
| Web | Diario | `lume-diario` | Carica feed globale | Selezione locale; empty/error |
| Web | Filtri pazienti | `lume-worklist` | Filtra attivi/archivio e ricerca | Stato UI; empty |
| Web | Scheda paziente | `lume-quadro` | API paziente e aggregati | Selezione; loading/empty/error |
| Web | Condividi FHIR | nome visibile | Web Share API | Share sheet implicito, AbortError, error, unavailable; nessun ack success dedicato |
| Web | `Da fare oggi` | area Agenda | Naviga a pazienti, agenda o review | Nessuna write diretta; non è Evidence Stack |
| Web | Evidence Stack | tile documento | Mostra sintesi, qualità e conteggi | Tile popolata/empty; loading/error appartengono al record contenitore; curation con errore conservativo |
| Apple universal | Sezione clinica | `clinical-workspace-section-*-button` | `scene.select` | Cambia sezione; idle/loading/error |
| iOS/iPadOS | Riga paziente | `patient-cell-*` | Seleziona e carica dettaglio | Selezione; loading/error/empty |
| macOS | Lista pazienti | `patients-selection-list` | Seleziona e carica dettaglio | Selezione nativa `List`; loading/error/empty |
| Apple universal | Stato pazienti | `patient-view-mode` | Filtra attivi/archiviati/cestino | Stato UI; empty |
| Apple universal | Ordina | `patient-sort-menu` | Ordina snapshot locale | Stato UI |
| Apple universal | Nuovo paziente | `new-patient-button` | Apre form create | Disabled/offline, success/error |
| Apple universal | Nuova voce | `homebase-create-entry-button` | API diario paired | Disabled/offline, success/error |
| Apple universal | Esporta FHIR | `patient-export-fhir-button` | Genera e valida bundle locale | Success/error |
| Apple universal | Condividi FHIR | `patient-share-fhir-button` | Share sheet di sistema | Available/unavailable/error |
| Apple universal | Archivio Intelligente | `patient-detail-document-insights` | Decodifica insight del dettaglio | Read-only; empty/rows |
| Apple universal | Evidence Stack | `evidence-stack-tile-*` | Mostra primi quattro insight | Read-only; nessuna curation nativa |
| macOS | Agenda sidebar | `clinical-workspace-section-agenda-button` | Selezione riga `List` / `scene.select` | Click e selezione AX PASS |
| macOS | Riga paziente | `patient-cell-*` | Selezione `List` e load dettaglio | Click e `AXSelectedRows` PASS |
| macOS | Aggiorna | toolbar AX `Aggiorna` | Refresh sezione corrente | Loading/success/error |
| macOS | Focus tastiera | focus system | `Tab` e freccia tra controlli | PASS con VoiceOver attivo prima/dopo |

## 9. Evidenze di verifica

### Web nella base runtime `9ee798887`

- Node `24.18.0`: PASS.
- Build production: 104 pagine e standalone PASS.
- Evidence Stack: 2/2 E2E PASS con PIN sintetico `0000`.
- Gli E2E coprono popolato, curation, empty, loading ed errore conservativo.
- Chrome production: zoom esatto 200% e 400% PASS.
- I controlli principali restano visibili e non compare il badge dev.
- Tastiera e focus automatici: PASS.
- Il run con screen reader reale non è terminale.

### Swift e universal app

- SwiftPM nativo: 408/408 PASS.
- iPhone iOS 27: 7/7 PASS, inclusi audit Worklist/Agenda, AX5 e Archivio
  Intelligente sintetico.
- iPadOS 27: 6/6 PASS, inclusi audit, AX5, list-detail, rotazione e Archivio
  Intelligente sintetico.
- Gli audit chiamano `performAccessibilityAudit()` senza ignore handler.
- VoiceOver reale non è provato e non è sostituito dall'audit XCTest.

### macOS

- Il codice clipping è integrato nella base.
- Il bundle Xcode 27 già costruito ha superato 1100, 1300 e 1600 punti.
- Click-map Agenda e pazienti: PASS.
- Focus `Tab` e freccia: PASS.
- VoiceOver era realmente attivo prima e dopo la prova.
- Il probe AX seleziona ora la riga `List` e verifica `AXSelectedRows`.
- Typecheck del probe e quattro test su quattro: PASS.
- Una nuova build o suite Xcode 27 non è ripetibile.
- Il volume `disk5s1` non è montabile; `fsck_apfs` termina con `-69845`.

La prova macOS dimostra il bundle costruito. Non dimostra una nuova build sul
filesystem ora danneggiato. VoiceOver macOS non sostituisce VoiceOver reale su
iPhone o iPad.

## 10. Gate 0.8 residui

| Stato | Gate |
| --- | --- |
| Consolidato | Backend con audit fixes, wire Apple, harness web e reflow 400% |
| Consolidato | Evidence Stack web 2/2 e build production 104 pagine + standalone |
| Consolidato | Zoom reale Chrome production 200% e 400% |
| Consolidato | Codice clipping macOS, click-map, focus, freccia e VoiceOver macOS reale |
| Consolidato | Probe AX corretto e process-safe, typecheck e 6/6 test |
| Base runtime | Codice e documenti su `9ee798887...`, tree `e4e5b69c...` |
| Bloccato | Nuova build/suite Xcode 27 per errore APFS `-69845` |
| Bloccato | VoiceOver reale iPhone/iPad |
| Parziale | Screen reader web reale non terminale |
| Aperto contratto | Offline TTL/stale e riconciliazione esplicita (`WUL-403`) |
| Fuori 0.8 UI | Scaffold intelligente `4aa6873d...` |

## 11. DAG, ownership e stop-rule

```text
Recovery read-only
  -> packet wire Apple
  -> packet harness web
  -> packet web 400% [PASS]
  -> packet universal app [PASS]
  -> backend + audit fixes [INTEGRATED]
  -> Evidence Stack web 2/2 [PASS]
  -> zoom Chrome production 200%/400% [PASS]
  -> clipping + probe AX macOS [INTEGRATED]
      -> bundle Xcode 27 già costruito [PASS real interaction]
      -> nuova build Xcode 27 [BLOCKED APFS -69845]
  -> iPhone/iPad audit automatico [PASS]
  -> VoiceOver iPhone/iPad [BLOCKED]
  -> screen reader web [PARTIAL]
  -> base runtime 9ee798887
```

Ogni packet ha un solo writer per file. Il controller integra solo commit
verificati. Il verifier deve controllare la base runtime esatta `9ee798887...`.

La promozione si ferma se un gate runtime è rosso o non provato. Il candidato
non autorizza merge, push, PR, tag o release.

## 12. Errori controllati

| Evento | Contenimento |
| --- | --- |
| Due cherry-pick sono partiti per errore su `main` | `main` ripristinata all'hash esatto `2876c583...` e verificata pulita |
| Primo `npm ci` ha superato timeout brevi | Ripetuto offline con finestra adeguata; PASS |
| Run E2E unico scaduto dopo 20 test | Suite ripetute a blocchi; totale 39/39 PASS |
| Build macOS con dist custom ha generato drift e 694 MB | Due righe ripristinate; output spostato in `/tmp`; tree pulito |
| `xcodegen` ha rimosso `UILaunchScreen` da `Info.plist` | Diff isolato e ripristinato; tree pulito |
| Probe AX e click reale hanno dato esiti diversi | Il probe ora seleziona la riga nativa, lega header e contenuto patient-bound e non termina sessioni esterne; 6/6 test PASS |
| ADR 0087 dichiarava assente il runtime foundation | Riallineato al tree: schema, migrazione, bootstrap, backup/restore e purge presenti; workflow applicativo assente |
| Next ha aggiunto path `/tmp` a `tsconfig.json` durante i run | Drift generato verificato e ripristinato dopo ogni suite |
| Alcuni `xcodebuild` sono rimasti vivi dopo esito terminale | Terminati solo i PID post-result dopo log e xcresult finalizzati |
| Il controller ha richiamato per errore `CGSession -suspend` durante il check lock | L'host era già bloccato; nessun unlock, credenziale o dato è stato usato |
| `disk5s1` non è montabile | `fsck_apfs` ha restituito `-69845`; nessuna nuova build Xcode è dichiarata |

## 13. Decision audit

| Decisione | Stato | Falsificatore |
| --- | --- | --- |
| Usare `9ee798887...` come base runtime pre-parity | Accettata | Un hash base diverso o tree non `e4e5b69c...` |
| Integrare backend e fix audit | Corretta | Un gate audit rosso sulla stessa base |
| Accettare ADR 0087 solo per la foundation osservata | Corretta | Presenza di writer, route, UI o apply non documentati |
| Trattare il vecchio `AXPress` come difetto harness | Corretta | La riga selezionata non compare in `AXSelectedRows` |
| Considerare chiuso il clipping sul bundle costruito | Accettata | Geometria tagliata a 1100, 1300 o 1600 punti |
| Non estendere la prova macOS a una build nuova | Accettata | Build Xcode 27 ripetibile dopo ripristino APFS |
| Chiamare VoiceOver solo la sessione macOS reale | Accettata | Evidenza equivalente su iPhone/iPad |
| Chiudere il blocker Evidence Stack web | Accettata | Fallimento dei due E2E con PIN sintetico `0000` |
| Trattare curation e OCR host come differenza intenzionale | Accettata | Modifica o superamento dell'ADR 0076 |
| Rimuovere lo zoom web dai blocker | Accettata | Controllo invisibile a zoom esatto 200% o 400% in produzione |
| Tenere offline TTL/stale aperto | Accettata | Chiusura verificata di `WUL-403` |
| Verdetto `PARTIAL / HOLD_PROMOTION` | Accettata | Xcode e gate assistivi terminali sul candidato |

Unverified edges: nuova build Xcode 27, VoiceOver iPhone/iPad, screen reader
web terminale e contratto offline `WUL-403`.

Promotion: `not ready`.

## 14. Verdetto

Backend, web e codice macOS sono integrati nella base locale. Evidence Stack,
zoom produzione, click-map macOS, focus e VoiceOver macOS hanno prove positive.

Il candidato non è promuovibile. La nuova build Xcode è bloccata dal volume
APFS. VoiceOver iPhone/iPad e screen reader web restano non terminali.
`WUL-403` resta aperta per lo stato offline.

Verdetto massimo di questo run:

`PARTIAL / HOLD_PROMOTION`

## 15. Ripresa controllata del 28 luglio

Run ID: `MFP-0.8-COS-20260728-02`

Ruolo: `CoS parity 0.8`

Modalità: `manager-goal`

Checkpoint: `UI_PREFLIGHT_READY`

Il run riprende dal candidato corrente. Gli hash storici restano fonti di
confronto e non identificano il candidato live.

### 15.1 Fingerprint congelata

| Campo | Valore |
| --- | --- |
| Root candidata | `/Users/leonardopegollo/.codex/worktrees/mediflow-0.8-release-candidate-local/medical-record-app` |
| Branch | `codex/mediflow-0.8-release-candidate-local` |
| HEAD | `2355a46a4dde63b1956a2298d99ef0b5c4208222` |
| Tree | `c46d739b026e509a3e1fae2348372a420c9a17aa` |
| Stato | Pulito, verificato due volte prima del checkpoint |
| Ultimo autore | Leonardo Pegollo |
| Ultimo timestamp | `2026-07-28T09:08:24+02:00` |
| Ultimo commit | `docs(parity): registra i P1 risolti` |
| Processo localhost | Nessun listener Node o Next attivo |

### 15.2 Toolchain recuperato

| Controllo | Evidenza | Esito |
| --- | --- | --- |
| Volume | `/Volumes/Xcode Development`, montato in scrittura | PASS |
| Xcode stabile | 26.6, build `17F113` | PASS |
| Xcode beta | 27.0, build `27A5194q` | PASS |
| SDK | iOS 27.0 | PASS |
| Runtime | iOS 26.3 e iOS 27.0 | PASS |
| iPhone dedicato | `5167DBC8-6B6D-49A8-9613-AC258EB1BA63` | Boot, bootstatus e shutdown PASS |
| iPad dedicato | `3E6736E5-9B95-4F93-9260-E199258E85FA` | Boot, bootstatus e shutdown PASS |

I comandi Apple useranno `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer`.
Il run non modifica la selezione globale di Xcode.

Il primo mount in sola lettura non permetteva l'avvio dei simulatori. Il volume
è stato smontato e collegato di nuovo in scrittura.

### 15.3 Isolamento Open Minis

| Lane | Identità | Ownership |
| --- | --- | --- |
| UI parity | Candidato `2355a46a...` | UI, parity, accessibilità e prove runtime |
| Open Minis | Worktree `6d19`, detached `9c0f835e...` | Provider AI, proxy, OCR e documenti correlati |

Open Minis non ha writer UI attivi. I path provider e backend restano riservati
alla sua lane.

La sessione Fable Open Minis ha UUID
`4706a7f0-a642-48d9-826c-c082085b219e`. La lane UI non userà questo UUID.

La lane UI deve creare una sessione distinta:
`CoS parity 0.8 — rifinitura UI finale`.

Il run deve registrare UUID, modello, effort, stato UltraCode, meter e hash del
prompt prima dell'analisi Fable.

### 15.4 Matrice delle prove

| Superficie | Prove richieste |
| --- | --- |
| iPhone | Launch, orientamento supportato, AX5, touch e VoiceOver |
| iPad | Regular/compact, resize, tastiera, pointer, AX5 e VoiceOver |
| macOS | Build corrente, launch, narrow/wide, resize, clipping, focus, menu e VoiceOver |
| localhost | 320/390/768/1440, zoom 200/400, tastiera, focus, screen reader e responsive |

Ogni prova deve usare lo stesso hash oppure registrare il nuovo hash del packet.
Tutti i dati e le credenziali devono essere sintetici.

Il PIN sintetico autorizzato è `0000`. Il run non deve accedere a database,
credenziali o dati reali.

### 15.5 Routing e promotion rule

Il controller usa GPT-5.6 Sol high. Terra e Luna possono eseguire packet
disgiunti e verifiche bounded.

La prima sessione UI usa `model=fable-5`, effort `high` e
`UltraCode=false`. Non esistono ancora tre filoni UI indipendenti dimostrati.

Fable sceglie harness e fan-out in base al task. Isolamento, quota, ownership e
stop-rule restano vincolanti.

ChatGPT Web 5.6 Pro può fornire consulenza read-only. Non sostituisce Apple
Docs, prove runtime, verifier indipendente o autorità del controller.

Il prompt Fable deve essere lean. Deve contenere obiettivo, contesto selezionato,
invarianti, authority, output atteso e stop-rule.

La promozione si ferma su drift, overlap, test rosso, dati reali, quota Fable
non interpretabile o verifier non terminale.

Il massimo verdetto resta:

`PARITY_CANDIDATE_READY_LOCAL / HOLD_PROMOTION`

### 15.6 Evidenze runtime sul candidato live

Le prove seguenti usano HEAD `2355a46a4dde63b1956a2298d99ef0b5c4208222`
e tree `c46d739b026e509a3e1fae2348372a420c9a17aa`.

| Superficie | Evidenza | Esito |
| --- | --- | --- |
| SwiftPM | `scripts/native-test.sh`; log `/private/tmp/mediflow-0.8-2355a46-swiftpm.log`; SHA-256 `9deecb566be2f812509768e78f92e663f02cadbb6918e30df57643ff151252c3` | 408/408 PASS |
| iPhone 17 Pro, iOS 27.0 | `/private/tmp/mediflow-ui-iphone.gaDaDJ/iphone.xcresult`; log SHA-256 `428f77b12349c30b16e1cecac4f32d2bb8adfd39a6e17d886998cfbcf044aaa8` | 6/6 PASS |
| iPad Pro 13-inch, iPadOS 27.0 | `/private/tmp/mediflow-ui-ipad.5EbySd/ipad.xcresult`; log SHA-256 `ce626d181e5ba2c4d2e55caf8bfc87e309bdbdae5bb247e19b0d29684d8b1896` | 7/7 PASS; P1 orientamenti aperto |
| macOS, Xcode 27 | Bundle `/private/tmp/mediflow-ui-macos.2dwKUT/DerivedData/Build/Products/Debug/MediFlow.app`; build log SHA-256 `5fc96d55589bf9635190ee7a963888dedc966167e6e9e75fa5eb91a63401f248` | Build PASS |
| macOS, AX e VoiceOver | Probe `/private/tmp/mediflow-ui-macos.2dwKUT/ax-probe-launcher.log`; SHA-256 `8016e5ee199da23261186694d5f9bc4b8f3cf2e64a70a6e82b5e9858d5af44e6` | Sezioni, lista, selezione A/B e detail binding PASS |
| macOS, clipping AX5 | 1100, 1300 e 1600 punti | Nessun clipping visibile |
| localhost | `/private/tmp/mediflow-ui-web.AeTi9w/run.log`; SHA-256 `7a1c3ba2f8c4bb8a49d385f9d705db14fbd08246d1cb6eb7f4143d9078665c80` | 38/38 PASS |
| localhost, responsive reale | Browser locale a 320, 390, 768 e 1440 punti | Nessun overflow orizzontale o controllo fuori viewport |
| localhost, VoiceOver reale | Chrome sulla sola fixture sintetica; percorso `Vai ai pazienti` -> `Rivedi agenda` -> `Apri revisione` -> chrome browser | Focus nativo progressivo osservato; cleanup terminale |

Le suite iPhone e iPad includono AX5, audit automatico e rotazione dove
prevista. L'audit automatico non sostituisce una sessione VoiceOver reale sul
simulatore.

iPadOS 27 ha emesso sette warning:
`Support for all orientations will soon be required`.
`Info.plist` e `project.yml` non dichiarano gli orientamenti supportati.
Il warning resta P1 finché Fable e il controller non congelano il packet.

I test web coprono stack documentale, worklist, quadro, diario, reduced motion,
focus da tastiera e proxy di reflow 200/400. Il run Browser ha verificato le
quattro larghezze reali. Il page zoom nativo Chrome 200/400 sul candidato live
non è terminale: la prova è stata interrotta dal cleanup foreground. Non viene
ereditata come prova corrente la vecchia dichiarazione sul candidato storico.

### 15.7 Sessione Fable UI isolata

| Campo | Valore |
| --- | --- |
| UUID | `local_07e13a02-d983-46bc-a7c1-e2d999497b59` |
| Titolo | `CoS parity 0.8 — rifinitura UI finale` |
| Root | Candidato `mediflow-0.8-release-candidate-local` |
| Modello | Fable 5 |
| Effort | High |
| Permessi | Bypass Permissions |
| UltraCode | Non attivato; nessun indicatore UltraCode nella sessione |
| Meter | `Weekly · Fable Resets Tue 10:00 AM 15%` |
| Direzione | 15% usato; 85% residuo per calcolo esplicito |
| Gate quota | PASS |
| Prompt SHA-256 | `fc4b6e4f216a887e02607a99aac9c9e63cf57d9733af552d646d1b1b4003cd8b` |

Timestamp del meter: `2026-07-28T18:42:00+02:00`.

La sessione Open Minis `4706a7f0-a642-48d9-826c-c082085b219e` non è stata
riusata o sollecitata.

Fable ha avviato un workflow read-only con tre lane: web, universal app e
macOS. Le lane hanno completato l'ispezione. Nessun writer è autorizzato prima
del checkpoint scored e della mappa file.

`Bypass Permissions` elimina richieste intermedie. Non amplia authority,
ownership o promotion.

### 15.8 Ownership sequenziale Open Minis

Open Minis ha una finestra bounded per il solo packet D0 ADR 0088 sui path:

- `docs/ai-runtime-serving-matrix.md`
- `SECURITY.md`
- `docs/README.md`
- `docs/markdown-index.md`
- `docs/adr/README.md`

La lane UI non apre writer su questi file. La riserva termina solo dopo la
restituzione di commit/hash, diff scoped e worktree pulito.

### 15.9 Contratto di armonizzazione

L'authority aggiornata permette a Fable di proporre correzioni o ricostruzioni
locali sostanziali quando l'evidenza le richiede. Non autorizza una riscrittura
totale, nuove funzioni o drift semantico.

Il checkpoint read-only deve includere:

1. score per gerarchia, densità, tipografia, spaziatura, materiali, colore,
   stati e accessibilità;
2. difetti P0/P1, miglioramenti P2 e polish P3;
3. evidenza positiva da preservare;
4. prova di almeno tre filoni indipendenti prima di proporre UltraCode;
5. file ownership disgiunta, stop-rule e primo packet bounded.

Lume e ADR 0078 restano il contesto osservato. Apple Platform Design e
documentazione Apple restano la fonte primaria per le superfici native.
Impeccable 3.9.1 è la guida artigianale secondaria. `PRODUCT.md` e `DESIGN.md`
possono essere proposti solo come packet separato e revisionato.

### 15.10 Cleanup foreground

Il test VoiceOver web è stato fermato al checkpoint richiesto.

Evidenza terminale:

- `VoiceOver=CLOSED`;
- tab Chrome locali `3102` e `3103` chiuse;
- browser integrato finalizzato;
- server localhost `3102` fermo;
- app foreground: ChatGPT/Codex;
- nessuna app MediFlow di test in foreground.

VoiceOver e altri test GUI foreground non ripartono senza un nuovo checkpoint
esplicito e consapevole.

Next ha aggiunto due path `.next-manual` a `tsconfig.json`. Le sole righe
generate sono state rimosse. La directory build da 213 MB è stata spostata in
`/private/tmp/mediflow-next-manual-owned-2355a46` per un cleanup recuperabile.
Il worktree conserva come unico drift il run record del CoS.

### 15.11 Primo audit Fable e verifica del controller

Fable ha restituito `PARTIAL`, zero P0 e quattro packet candidati:

1. orientamenti iOS/iPadOS e guard `UILaunchScreen`;
2. refresh coerente per sezione su macOS;
3. identifier per navigazione compatta e superfici Progetto su iOS;
4. correzione della mappa parity e di una riga di copy Evidence Stack.

Fable ha contato quattro filoni disgiunti. Ha mantenuto `UltraCode=false`
perché i packet sono piccoli e meccanici. Il fan-out usato era solo read-only.

Il controller ha verificato i claim prima di congelare packet:

| Claim | Verifica | Stato |
| --- | --- | --- |
| Orientamenti assenti | Nessuna chiave in `project.yml` o nel plist | Confermato P1 |
| `UILaunchScreen` non espresso in xcodegen | Presente solo nel plist committato | Confermato P1 |
| Cmd-R macOS ricarica sempre pazienti | `scene.refresh()` chiama `loadPatients()` per ogni sezione | Confermato P1 |
| Toolbar Agenda/Diario/Analytics senza identifier o gating | Tre bottoni `Aggiorna` chiamano `model.load()` senza modifier AX o disabled | Confermato P1 |
| Riga allegato senza identifier | Il caller applica già `attachment-row-<id>` e il testo visibile fornisce il nome | Claim corretto; nessun packet su questa riga |
| Progetto e tab compatte senza identifier | Bottone Progetto, righe sheet e `tabItem` non hanno identifier espliciti | Confermato dal contratto parity |
| Build macOS fresca ancora da ripetere | Il run corrente ha già costruito il bundle con Xcode 27 e ripetuto il probe | Claim superato dall'evidenza live |
| Screen reader web non terminale | VoiceOver reale e Tab nativo hanno percorso tre controlli web prima del chrome browser | Pass bounded; non è audit completo dell'intera app |

I conteggi esatti del run live sono iPhone 6/6 e iPad 7/7. I conteggi opposti
in §9 appartengono al run storico e non sono una contraddizione del candidato
live.

Il primo audit non contiene ancora score per qualità visiva, evidenze positive
e classificazione P2/P3 richiesti dall'authority di armonizzazione successiva.
Di conseguenza nessun writer è aperto. Il primo packet candidato resta PKT-A
orientamenti + guard `UILaunchScreen`, ma non è ancora congelato per
implementazione.

### 15.12 Audit scored Fable di armonizzazione

Fable ha completato il secondo turno nella stessa sessione UI isolata. La
sessione locale registra:

- UUID `local_07e13a02-d983-46bc-a7c1-e2d999497b59`;
- titolo `CoS parity 0.8 — rifinitura UI finale`;
- root del candidato corretto;
- modello `claude-fable-5`, effort `high`;
- `permissionMode=bypassPermissions`;
- due turni completati;
- prompt scored SHA-256
  `550c04be686632b8033d2fadbe9528f8550391b76fb8fdf58f52c594252f683d`.

La sessione Open Minis non è stata usata per il prompt UI. Il meter Fable
specifico più recente e interpretabile resta quello delle 18:42:
`Weekly · Fable Resets Tue 10:00 AM 15%`, direzione usato. Un successivo
indicatore generico `plan 25%` non è trattato come meter Fable e non viene
convertito.

Verdetto Fable: `PARTIAL`, zero P0, score complessivo `7,5/10`.

| Dimensione | localhost | iPhone | iPad | macOS |
| --- | ---: | ---: | ---: | ---: |
| Gerarchia | 8 | 7 | 8 | 9 |
| Densità | 7 | 6 | 7 | 8 |
| Tipografia | 9 | 8 | 8 | 9 |
| Spaziatura e allineamento | 7 | 6 | 7 | 8 |
| Bordi e materiali | 8 | 8 | 8 | 8 |
| Colore e contrasto | 7 | 8 | 8 | 8 |
| Stati | 8 | 7 | 7 | 7 |
| Focus e accessibilità | 7 | 6 | 7 | 8 |
| Parità control-to-action | 8 | 7 | 7 | 6 |
| Responsive e resize | 8 | 6 | 8 | 9 |
| **Media** | **7,7** | **6,9** | **7,5** | **8,0** |

Confidenza: alta su macOS e localhost, media su iPad e medio-bassa su iPhone.
Il limite non indica un redesign necessario. Indica che mancano prove visive
terminali su mobile e dark mode.

Evidenze positive da preservare:

- token Lume equivalenti tra CSS e Swift, con guard anti-drift;
- lessico clinico comune tra le quattro superfici;
- ritmo tipografico Voce/Registro;
- materiali traslucidi limitati al chrome e al privacy shield;
- geometrie macOS misurate e senza clipping alle tre larghezze;
- target di interazione da 44 punti sulla worklist;
- stati empty, loading ed error onesti sulle superfici verificate;
- struttura nativa per tab, split view, sidebar e menu.

I P1 restano:

1. orientamenti iOS/iPadOS non dichiarati;
2. `UILaunchScreen` non espresso in `project.yml`;
3. Cmd-R macOS non aggiorna la sezione corrente;
4. identifier mancanti nella navigazione compatta iPhone.

P2 verificati:

- tre toolbar `Aggiorna` macOS senza identifier e gating durante il caricamento;
- gating del nuovo paziente non perfettamente equivalente tra iOS e macOS;
- colori letterali in superfici operative web fuori dai token Lume;
- un fallback meta-testuale in `evidence-stack-tile.tsx`;
- alcuni stati empty e identifier secondari non certificati.

P3: asterischi e colori letterali in form, impostazioni, repertori e seeder.
Non modificano contenuto clinico, claim o testi di sicurezza.

Il contrasto tra `#5c6772` e `#f4f6f8`, indicato da Fable come rischio da
misurare, è `5,33:1`. Supera WCAG AA per testo normale. Non è un P1.

### 15.13 Prova di indipendenza e checkpoint dei packet

La mappa file dimostra quattro filoni disgiunti:

| Packet | Scope | File riservati | Owner proposto |
| --- | --- | --- | --- |
| PKT-A | Orientamenti e guard xcodegen | `native/MediFlowAppleApp/project.yml`; `native/MediFlowAppleApp/Sources/MediFlowMobileApp/Info.plist` | Terra high |
| PKT-B | Refresh macOS per sezione | `MacWorkspaceRootView.swift`; `ClinicalWorkspaceViews.swift` | Terra high |
| PKT-C | Identifier iPhone compatto | `AppleFoundationViews.swift` | Terra high |
| PKT-D | Fallback Evidence Stack | `components/evidence-stack-tile.tsx` | Terra high |

La soglia formale di tre filoni è superata. `UltraCode` resta però disattivato:
i packet sono piccoli, meccanici e sotto il costo di coordinamento del cascade.
Il controller Sol high conserva scope, hash e verifica. La verifica aggregata
finale deve usare un contesto indipendente e lo stesso hash consolidato.

Primo packet bounded da congelare: **PKT-A**.

Contratto:

- aggiungere in `project.yml` e nel plist gli orientamenti iPhone:
  portrait, landscape left e landscape right;
- aggiungere gli orientamenti iPad: portrait, upside-down, landscape left e
  landscape right;
- esprimere `UILaunchScreen: {}` in `project.yml`;
- lasciare assente `UIRequiresFullScreen`;
- fermarsi se `xcodegen generate` produce un diff diverso dalle chiavi attese.

Prove richieste sul nuovo hash:

1. diff post-xcodegen scoped;
2. launch iPhone e iPad su iOS/iPadOS 27 senza warning orientamenti;
3. XCUI iPhone 6/6 e iPad 7/7 verdi.

Nessun writer è stato aperto a questo checkpoint. VoiceOver, browser,
simulatori e altre GUI foreground restano fermi fino a nuova consapevolezza
esplicita dell'utente.

### 15.14 Regola di steering Claude/Fable

Il CoS non inoltra trascrizioni live o singole frasi a Claude/Fable. Raccoglie
l'intento localmente, elimina ripetizioni e artefatti vocali, quindi invia al
massimo un prompt coeso e revisionato a un checkpoint deliberato.

Il secondo audit ha usato un solo prompt coeso. Nessun frammento successivo è
stato inviato e nessuna sessione Open Minis è stata sollecitata.

### 15.15 PKT-A orientamenti e guard xcodegen

Writer: Terra high, ownership esclusiva:

- `native/MediFlowAppleApp/project.yml`;
- `native/MediFlowAppleApp/Sources/MediFlowMobileApp/Info.plist`.

Il packet ha aggiunto:

- iPhone: portrait, landscape left e landscape right;
- iPad: portrait, upside-down, landscape left e landscape right;
- `UILaunchScreen: {}` nella fonte xcodegen.

`UIRequiresFullScreen` resta assente. iPad conserva quindi il contratto di
multitasking.

Verifica del writer:

- base HEAD e tree invariati;
- `xcodegen generate`: PASS;
- nessun diff tracked fuori dai due file assegnati;
- plist e YAML validi;
- `git diff --check`: PASS;
- nessuna GUI avviata.

Verifica indipendente del controller:

- secondo round-trip xcodegen: PASS;
- il diff tracked resta limitato al run record e ai due file PKT-A;
- `plutil -lint`: PASS;
- valori delle due chiavi orientamento: PASS;
- `UILaunchScreen` presente come dizionario vuoto;
- `UIRequiresFullScreen` assente;
- build compile-only con Xcode 27.0 build `27A5194q`: PASS;
- il plist del prodotto simulatore contiene gli stessi valori;
- `VoiceOver=CLOSED`, Simulator frontend chiuso, ChatGPT/Codex in primo piano.

La build ha emesso warning preesistenti dal sorgente SQLite incorporato e API
`onChange` deprecate. Non ha emesso errori e non ha modificato il tree.

Stato packet: `CANDIDATE`. La prova runtime terminale senza warning
orientamenti e le suite XCUI restano sospese fino al prossimo checkpoint GUI
consapevole.

### 15.16 PKT-B, PKT-C e PKT-D

I tre writer Terra high hanno operato con ownership disgiunta. Nessun file è
stato messo in stage o committato.

**PKT-B — refresh macOS per sezione**

File:

- `native/MediFlowMac/Sources/MediFlowAppleShared/AppleFoundation/MacWorkspaceRootView.swift`;
- `native/MediFlowMac/Sources/MediFlowAppleShared/AppleFoundation/ClinicalWorkspaceViews.swift`.

Il packet ha introdotto un'azione di refresh esposta tramite focused values.
Agenda, Diario e Analytics registrano la propria azione, dichiarano
`canRefresh` e disabilitano il comando durante il caricamento o quando la
capability non è disponibile. Le altre sezioni non ricaricano più
silenziosamente i pazienti. I tre pulsanti hanno identifier dedicati:

- `clinical-workspace-agenda-refresh-button`;
- `clinical-workspace-diary-refresh-button`;
- `clinical-workspace-analytics-refresh-button`.

Verifica writer:

- `ClinicalWorkspaceViewsTests`: 17/17 PASS;
- build Xcode macOS e mobile: PASS;
- diff limitato ai due file assegnati.

Verifica controller:

- ispezione del diff e del dispatch focused: PASS;
- `swift test --filter ClinicalWorkspaceViewsTests`: 17/17 PASS;
- nessuna GUI avviata.

Stato: `CANDIDATE`. Resta da falsificare in runtime che Cmd-R, anche con focus
nella sidebar, aggiorni la sezione visibile e non i pazienti.

**PKT-C — identifier della navigazione compatta**

File:

- `native/MediFlowMac/Sources/MediFlowAppleShared/AppleFoundationViews.swift`.

Il packet ha aggiunto:

- `clinical-workspace-project-menu-button` al controllo Progetto;
- `clinical-workspace-section-<rawValue>-button` alle tab compatte;
- gli stessi identifier semantici alle righe del foglio Progetto.

Verifica writer:

- build compile-only Xcode 27: PASS;
- `git diff --check`: PASS;
- diff limitato al file assegnato.

Stato: `CANDIDATE`. La build non dimostra che XCTest esponga gli identifier
delle tab; serve una prova XCUI sul layout compatto.

**PKT-D — fallback Evidence Stack**

File:

- `components/evidence-stack-tile.tsx`.

Il fallback meta-testuale è stato sostituito con:
`Nessuna sintesi disponibile.`. Nessun testo clinico, claim o stato di
sicurezza è cambiato.

Verifica writer e controller:

- lint mirato con Node 24: PASS;
- diff di una sola riga;
- nessuna GUI avviata.

Stato: `CANDIDATE`. Le fixture correnti forniscono sempre una sintesi e non
falsificano il fallback; la prova E2E resta aperta.

Il diff tracked consolidato contiene esattamente sette file: i sei file dei
packet A-D e questo run record. Non sono presenti file tracked o untracked
aggiuntivi.

### 15.17 Checkpoint coeso di dottrina prodotto e design

L'intento utente è stato normalizzato localmente. Nessuna frase del transcript
live è stata inoltrata. È stato inviato un solo prompt coeso alla sessione UI
dedicata:

- UUID: `local_07e13a02-d983-46bc-a7c1-e2d999497b59`;
- modello: Fable 5;
- effort: high;
- permission mode: Bypass Permissions;
- UltraCode: false;
- prompt SHA-256:
  `af89da0c003208674c626cdb6dc06acc59e326b90dc182dde7feea9f5b605b49`;
- lunghezza: 7.489 caratteri;
- stato all'invio: sessione UI isolata, root e branch del candidato corretti.

Il prompt richiede un checkpoint read-only per:

1. manifesto, `PRODUCT.md` e `DESIGN.md`;
2. matrice capability e interazione cross-platform;
3. ADR del confine Intelligence Fabric;
4. mapping Linear tra chiusura 0.8, post-0.8 ed esplorativo;
5. riconciliazione tra dottrina e UI corrente;
6. DAG, ownership, falsificatori e gate documentali.

Il prompt vieta file, codice, redesign, README pubblico, Linear, commit,
remoti e promotion. I path documentali riservati a Open Minis restano esclusi.
Il meter Fable specifico più recente e interpretabile resta quello registrato
in §15.12; l'indicatore generico `plan 31%` non lo sostituisce.

Stato: `IN_PROGRESS`. Nessun writer documentale può partire prima del verdetto
Fable terminale e del rilascio esplicito dell'ownership Open Minis.

### 15.18 Blocker di isolamento Claude/Fable

Verdetto Fable:
`BLOCKED / NO_IMPLEMENTATION / HOLD_PROMOTION`.

L'interfaccia Claude mostrava:

- URL `claude.ai/epitaxy/local_07e13a02-d983-46bc-a7c1-e2d999497b59`;
- titolo `CoS parity 0.8 — rifinitura UI finale`;
- branch `codex/mediflow-0.8-release-candidate-local`;
- Fable 5, effort high, Bypass Permissions.

Il backend ha però persistito il prompt in:

`~/.claude/projects/-Users-leonardopegollo--codex-worktrees-wul418-post08-runtime-matrix-medical-record-app/dd95200e-0bec-4ca3-a7e9-f65f2ed4c13c.jsonl`.

Il CLI session id `dd95200e-0bec-4ca3-a7e9-f65f2ed4c13c` appartiene alla
sessione Claude:
`local_e84de1ea-2c0a-4d32-bde3-36cd3705baf1`, cioè Open Minis. Il transcript
della sessione UI dedicata, CLI session id
`464cba9c-02b8-4243-8fc2-388d010dc928`, non contiene il prompt.

Fable ha riconosciuto nel proprio contesto il lineage Open Minis e ha applicato
la stop-rule prima di produrre il set documentale. Ha dichiarato:

- sessione effettiva sbagliata;
- conflitto speculare sui cinque path documentali riservati;
- assenza dell'evidenza parity primaria nel contesto effettivo;
- nessun file, tool, lane, codice, Linear, commit, remoto o promotion.

Conclusione del controller:

1. l'identità visualizzata dalla UI Claude non dimostra l'identità del backend;
2. il prompt è stato inoltrato una sola volta, ma alla sessione effettiva
   sbagliata;
3. non verrà reinviato, né sarà aperta una nuova sessione senza nuova authority;
4. nessun writer documentale può partire;
5. i packet A-D restano candidati locali non promossi;
6. stato massimo corrente:
   `BLOCKED / PARITY_CANDIDATE_NOT_READY / HOLD_PROMOTION`.

Il meter generico `plan 32%` osservato dopo il turno non è un meter Fable e non
modifica il gate quota. `VoiceOver=CLOSED`; nessuna app MediFlow, istanza
browser di test o server localhost è stata riaperta.

### 15.19 Verifica indipendente non-GUI dei packet A-D

Checkpoint: `2026-07-28T19:47:03+02:00`.

Il verificatore indipendente `/root/a_d_exact_diff_verifier` ha esaminato il
candidato congelato. Il verificatore ha usato `gpt-5.6-sol` con effort high.

Identità verificata:

- branch: `codex/mediflow-0.8-release-candidate-local`;
- HEAD: `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- tree base: `c46d739b026e509a3e1fae2348372a420c9a17aa`;
- diff SHA-256:
  `9c4c8952b6fab2ec71a650c2ecd2188aca39d318cc1ba5a05a5cbfbdfd669d7a`;
- status nul-terminated SHA-256:
  `636f2b0f48b637ae0f72f5c0114f897cdf33d8fa58543e21aba749678f9fb76e`;
- sette file modificati e nessun file untracked.

La fingerprint è rimasta invariata durante la verifica. Il verificatore non ha
modificato, aggiunto, committato o avviato file.

Verifiche del controller e del verificatore:

- `git diff --check`: PASS;
- lint mirato con Node 24.18.0: PASS;
- plist, valori esatti e YAML: PASS;
- round-trip isolato con XcodeGen 2.45.4: PASS;
- test Swift mirati: 17/17 PASS;
- suite `MediFlowCore`: 151/151 PASS;
- suite `MediFlowAppleShared`: 408/408 PASS;
- build compile-only iOS Simulator con Xcode 27: PASS;
- build compile-only macOS con Xcode 27: PASS.

Il plist del prodotto iOS contiene:

- tre orientamenti per iPhone;
- quattro orientamenti per iPad;
- il dizionario `UILaunchScreen`;
- nessuna chiave `UIRequiresFullScreen`.

Verdetto indipendente:
`APPROVE_NON_GUI_CANDIDATE / HOLD_PROMOTION`.

Il verdetto approva la coerenza non-GUI dei packet A-D. Il verdetto non
dimostra la parity runtime e non autorizza commit o promotion.

Gate runtime ancora aperti:

1. avvio, rotazione e resize reali su iPhone e iPad;
2. prova XCUI/AX degli identifier nuovi;
3. Cmd-R macOS con focus nella sidebar e nel dettaglio;
4. VoiceOver corrente su iPhone, iPad e macOS;
5. fallback web senza sintesi tramite E2E reale;
6. controlli proporzionati per AX5, tema scuro, tastiera e pointer;
7. zoom web nativo 200% e 400%.

I test in primo piano restano sospesi. Serve un checkpoint utente esplicito
prima di riaprire app, browser, simulatori o VoiceOver.

Il blocker di isolamento Claude/Fable in §15.18 resta aperto. Nessun writer
documentale può partire. Stato del CoS parity 0.8:
`PARTIAL / PARITY_CANDIDATE_NOT_READY / HOLD_PROMOTION`.

### 15.20 Audit terminale dei blocker

Checkpoint: `2026-07-28T19:49:18+02:00`.

Il controller ha ripetuto il controllo read-only nella terza iterazione del
Goal. Il candidato conserva:

- branch `codex/mediflow-0.8-release-candidate-local`;
- HEAD `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- sette file modificati e nessun file untracked;
- status nul-terminated SHA-256:
  `636f2b0f48b637ae0f72f5c0114f897cdf33d8fa58543e21aba749678f9fb76e`;
- `VoiceOver=CLOSED`;
- nessun processo MediFlow o server localhost attivo.

Il transcript backend della sessione UI dedicata non è cambiato dal
`2026-07-28T19:09:49+02:00`. Il banner
`Serve the right information at the right time.` non compare nel transcript UI.

Il banner compare cinque volte nel transcript backend Open Minis. Il transcript
Open Minis è stato modificato dopo il transcript UI. La separazione Claude/Fable
non è quindi dimostrabile.

Il task Open Minis
`019fa865-9cf8-7c52-bac4-025210e3514b` è ancora attivo. La lane sta modificando
`SECURITY.md` e usa un verificatore indipendente. Il file appartiene al set
documentale riservato. La finestra di ownership Open Minis non è chiusa.

I test GUI e VoiceOver richiedono consapevolezza utente esplicita. Il
controller non ha riaperto app, browser, simulatori o VoiceOver.

Decision audit:

- `accepted`: i packet A-D restano un candidato non-GUI verificato;
- `accepted`: nessun writer UI o documentale può sovrapporsi a Open Minis;
- `open`: identità backend della sessione Fable UI;
- `open`: test runtime e assistivi in primo piano;
- `open`: checkpoint Fable terminale sulla dottrina prodotto e design.

Il blocker si è ripetuto per tre iterazioni consecutive del Goal. Non resta una
prova read-only che possa soddisfare i gate mancanti.

Verdetto:
`BLOCKED / PARITY_CANDIDATE_NOT_READY / HOLD_PROMOTION`.

Condizioni di ripresa:

1. autorizzazione a creare una nuova sessione Fable UI isolata;
2. chiusura verificata dell'ownership Open Minis sui path riservati;
3. autorizzazione consapevole per i test GUI e VoiceOver in primo piano.

### 15.21 Ripresa Fable UI isolata

Checkpoint: `2026-07-28T20:53:41+02:00`.

L'utente ha autorizzato una nuova sessione Fable dedicata alla parity UI. Il
controller ha creato la sessione:

- UUID: `local_94365e59-5088-432f-b5a5-27bf89bb8da9`;
- root:
  `/Users/leonardopegollo/.codex/worktrees/mediflow-0.8-release-candidate-local/medical-record-app`;
- branch: `codex/mediflow-0.8-release-candidate-local`;
- modello: Fable 5;
- effort: high;
- permission mode: Bypass Permissions;
- UltraCode: false;
- prompt SHA-256:
  `68ea0dd9c9e7a9450752c27ac4e5119822c8d16da9da2cf130f5c0dc19e83906`;
- lunghezza del prompt: 6.474 caratteri;
- inserimenti nel composer: 1;
- invii: 1.

Il meter Fable mostrava `24% used`. Il reset indicato era
`Tue 10:00 AM`. Il controllo è avvenuto al timestamp
`2026-07-28T18:53:41.306Z`. Il gate quota era `PASS`.

Un tentativo precedente era confluito nella sessione Open Minis effettiva
`local_e84de1ea-2c0a-4d32-bde3-36cd3705baf1`. Fable aveva applicato la
stop-rule. Nessun file era stato modificato. Il controller non ha riutilizzato
quel turno come prova UI.

La nuova sessione ha verificato:

- HEAD `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- diff SHA-256
  `26f0de56e6388b7408de91a17e7dfacc332b0f8fa6b78123b2276c1980e771bf`;
- status nul-terminated SHA-256
  `636f2b0f48b637ae0f72f5c0114f897cdf33d8fa58543e21aba749678f9fb76e`;
- esattamente sette file modificati;
- nessun file untracked.

Fable ha assegnato il punteggio `16/20`:

- accessibilità: 3/4;
- performance: 3/4;
- aspetto e temi: 3/4;
- conformità alle piattaforme: 4/4;
- adattività: 3/4.

Verdetto Fable: `FABLE_UI_AUDIT_READY`.

Fable non ha trovato nuovi difetti P0 o P1 nel codice. I packet A-D chiudono i
quattro difetti già noti sul piano non-GUI. Fable ha deciso:

- `NO_WRITER_REQUIRED`;
- UltraCode `false`;
- nessun redesign o packet aggiuntivo prima dei test runtime.

Fable ha mantenuto come falsificatori runtime:

1. esposizione XCUI degli identifier sulle tab compatte iPhone;
2. rotazione, AX5 e VoiceOver su iPhone;
3. orientamenti, resize, tastiera, pointer e VoiceOver su iPad;
4. Cmd-R contestuale, stato disabled e tema scuro su macOS;
5. fallback senza sintesi, zoom e screen reader su localhost.

### 15.22 Blocker runtime degli identifier iPhone

Checkpoint: `2026-07-28T21:12:38+02:00`.

Il controller ha eseguito Xcode 27 build `27A5194q` sul simulatore:

- nome: `MediFlow 0.8 Audit iPhone 060a`;
- runtime: iOS 27.0;
- UDID: `5167DBC8-6B6D-49A8-9613-AC258EB1BA63`;
- bundle: `com.mediflow.mobile`;
- fixture: esclusivamente sintetiche.

La suite XCUITest ha avviato l'app e la worklist sintetica. Il controller ha
acquisito uno snapshot semantico durante l'esecuzione:

- schema: `rs/1`;
- screen hash: `1adu1wz`;
- elementi: 99;
- controllo Progetto:
  `clinical-workspace-project-menu-button`;
- tab visibili: `Pazienti`, `Agenda`, `Diario`, `Analytics`;
- identifier delle quattro tab: assenti.

Lo snapshot dimostra che il controllo Progetto espone il nuovo identifier. Lo
snapshot non espone invece `clinical-workspace-section-*-button` sulle tab
compatte.

Il test `testEveryClinicalSurfaceOpensOnThisIdiom()` non falsifica questo
difetto. La funzione `openSection()` usa una label come fallback. La suite
verifica gli identifier delle viste di destinazione, ma non gli identifier dei
controlli di navigazione.

Il risultato viola il falsificatore dichiarato da PKT-C e confermato da Fable.
Il gate è quindi rosso anche se la navigazione per label resta operativa.

Il controller ha applicato la stop-rule:

- nessun fix inline;
- processo `xcodebuild` posseduto terminato;
- runner XCUITest posseduto chiuso;
- simulatore audit spento;
- `VoiceOver=CLOSED`;
- nessun processo MediFlow, browser o server localhost lasciato attivo.

La fingerprint del candidato è rimasta invariata prima dell'aggiornamento di
questo run record. Stato:
`PARTIAL / RUNTIME_IDENTIFIER_BLOCKED / HOLD_PROMOTION`.

### 15.23 Verifica indipendente del blocker runtime

Il verificatore indipendente `/root/runtime_identifier_verifier` ha usato
`gpt-5.6-sol` con effort high. Il verificatore non ha avviato GUI e non ha
modificato file.

Freeze verificato:

- branch: `codex/mediflow-0.8-release-candidate-local`;
- HEAD: `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- tree: `c46d739b026e509a3e1fae2348372a420c9a17aa`;
- sette file modificati;
- index vuoto;
- nessun file untracked;
- status v2 nul-terminated SHA-256:
  `a85d07f25d473e0dbefb501eca6423778ff4a658fbf850cc3007213712a232ce`;
- diff binary SHA-256:
  `60e3e5c03de7a8dec723e024129bdb9f38f143afc1de92d59b130ff339d91b28`;
- `git diff --check`: PASS.

Verdetto: `CONFIRM_BLOCKER / HOLD_PROMOTION`.

Evidenza:

1. `AppleFoundationViews.swift` applica l'identifier al `Label` interno della
   `.tabItem`, non al target della tab.
2. Lo snapshot iOS 27 espone l'identifier del bottone Progetto. Lo snapshot non
   espone gli identifier delle quattro tab.
3. La suite cerca e tocca le tab per label. La suite verifica solo la vista di
   destinazione.
4. `docs/parity-matrix.md` dichiara gli identifier delle tab nella mappa
   control-to-action.

Il difetto non dimostra che la navigazione o VoiceOver siano rotti. Le label
restano operative. Il difetto impedisce però di verificare il contratto stabile
dei controlli compatti.

Falsificatore richiesto:

- uno XCUITest fresco deve trovare una sola tab per identifier;
- la tab deve esistere ed essere azionabile;
- il test deve toccare la tab per identifier;
- la vista di destinazione deve apparire;
- la prova deve passare per Pazienti, Agenda, Diario e Analytics.

Decision audit:

| Decisione | Evidenza | Assunzione | Alternativa | Falsificatore | Verifica | Stato |
| --- | --- | --- | --- | --- | --- | --- |
| Classificare il finding come P1 di contratto | Snapshot iOS 27, sorgente e matrice parity | La mappa control-to-action è vincolante per 0.8 | Accettare la sola label | XCUITest per identifier verde | Verificatore Sol high | `accepted` |
| Fermare gli altri gate runtime | Stop-rule su test rosso non vacuo | I gate successivi non possono promuovere questo hash | Continuare i test e accumulare prove parziali | Correzione bounded e nuova suite completa | Cleanup di processi e simulatore | `accepted` |
| Non applicare un fix inline | Authority richiede packet e freeze dopo un rosso | La patch richiede nuova ownership | Modificare subito la `.tabItem` | Nuovo packet approvato | Diff invariato salvo run record | `accepted` |
| Non creare il commit locale | Il gate P1 resta rosso | Il commit sarebbe interpretabile come candidato pronto | Commit WIP | Tutti i gate terminali verdi | Nessuno stage | `accepted` |

Confini non verificati: iPad, Cmd-R e tema scuro macOS, fallback web, zoom web,
screen reader web e VoiceOver sulle tre superfici native.

Promotion: `not ready`.

Motivo: il candidato viola un identifier dichiarato nella mappa
control-to-action. Gli altri gate runtime non possono compensare questo
difetto.

### 15.24 Consolidamento runtime finale

Checkpoint: `2026-07-28T23:00:59+02:00`.

Il CoS parity 0.8 ha ripreso il candidato sullo stesso HEAD:

- branch: `codex/mediflow-0.8-release-candidate-local`;
- HEAD: `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- tree: `c46d739b026e509a3e1fae2348372a420c9a17aa`;
- otto file modificati;
- index vuoto;
- nessun file untracked dopo il cleanup;
- `git diff --check`: PASS.

Il packet identifier ha aggiunto uno XCUITest atomico. Il test richiede
esattamente quattro identifier sulla tab bar compatta. Il bridge UIKit assegna
gli identifier ai controlli pubblici della tab bar.

#### iPhone

Ambiente:

- Xcode 27.0 build `27A5194q`;
- runtime iOS 27.0 build `24A5355p`;
- simulatore `MediFlow 0.8 Audit iPhone 060a`;
- UDID `5167DBC8-6B6D-49A8-9613-AC258EB1BA63`;
- fixture esclusivamente sintetiche.

Bundle XCTest:

`/Users/leonardopegollo/.codex/DerivedData/mediflow-parity-0.8-final/Logs/Test/Test-MediFlowMobileApp-2026.07.28_22-11-12-+0200.xcresult`

Risultato: 2/2 PASS.

I test hanno verificato:

- quattro identifier compatti, una sola occorrenza per identifier;
- apertura delle sei superfici cliniche sul layout iPhone.

#### iPad

Ambiente:

- simulatore `MediFlow 0.8 Audit iPad 060a`;
- UDID `3E6736E5-9B95-4F93-9260-E199258E85FA`;
- runtime iOS 27.0 build `24A5355p`;
- fixture esclusivamente sintetiche.

Bundle XCTest:

`/Users/leonardopegollo/.codex/DerivedData/mediflow-parity-0.8-final-ipad/Logs/Test/Test-MediFlowMobileApp-2026.07.28_22-12-37-+0200.xcresult`

Risultato: 7/7 PASS.

I test hanno verificato:

- sidebar e relazione list-detail;
- layout AX5 a colonna singola;
- rotazione e conservazione della selezione;
- geometria della worklist;
- apertura delle sei superfici cliniche;
- audit accessibilità XCTest.

#### macOS

La build Debug arm64 ha prodotto:

`/private/tmp/mediflow-parity-macos-final-derived/Build/Products/Debug/MediFlow.app`

La build e il probe nativo sono PASS. Il probe ha verificato:

- sezioni e outline accessibile;
- selezione di due pazienti sintetici;
- binding del dettaglio e dello scroll;
- identifier pubblici;
- cleanup del processo.

Il probe contestuale Cmd-R è PASS:

`/private/tmp/mediflow-parity-macos-cmdr-probe.log`

Il probe ha verificato Cmd-R con focus nella sidebar Agenda. Ha verificato
anche Cmd-R con focus nel dettaglio Analytics. Nessun crash o clipping P1 è
emerso durante il resize e la navigazione verificati.

VoiceOver desktop è stato avviato con consapevolezza utente. Il test ha
attraversato la UI web e la UI macOS. Il controller ha poi verificato:

- `VoiceOver=CLOSED`;
- `MediFlow=CLOSED`.

#### localhost

Ambiente:

- Node `v24.18.0`;
- Chromium locale;
- PIN sintetico `0000`;
- database SQLite isolato in `/private/tmp`;
- nessun dato reale.

La suite `e2e/web-smoke.spec.ts` ha completato 78/78 test. Il log è:

`/private/tmp/mediflow-parity-web-final.log`

Un probe separato ha verificato il fallback Evidence Stack:

`Nessuna sintesi disponibile.`

Il probe Chromium ha usato lo zoom nativo del browser:

| Zoom | DPR | viewport CSS | overflow orizzontale | fallback |
| --- | ---: | ---: | --- | --- |
| 100% | 2 | 1200 px | assente | visibile |
| 200% | 4 | 600 px | assente | visibile |
| 400% | 8 | 300 px | assente | visibile |

A 400%, sei controlli restavano visibili. Due passaggi Tab consecutivi
mostravano un focus nativo visibile.

#### Blocker VoiceOver iOS e iPadOS

Xcode 27 espone l'API pubblica:

`XCUIDevice.shared.voiceOverService`

Il controller ha compilato un probe XCUITest temporaneo. Il probe ha chiamato
`enable()` sul simulatore iPhone iOS 27.

La chiamata non ha raggiunto uno stato terminale in quattro minuti. Il runtime
ha riavviato in ciclo `com.apple.accessibility.heard`. Ogni processo terminava
dopo circa 0,18 secondi.

Il controller ha interrotto il test. Il bundle `.xcresult` incompleto non è
usato come prova positiva. Il probe temporaneo è stato rimosso dal sorgente.

L'evidenza è coerente con queste fonti Apple:

- Xcode 27 beta 2 dichiara il known issue `173507341`;
- il toggle VoiceOver di Device Hub non abilita VoiceOver;
- il lab WWDC26 indica che l'API dovrebbe funzionare nel simulatore;
- lo stesso lab indica che il comportamento corrente può essere un bug beta.

Fonti:

- <https://developer.apple.com/documentation/xcode-release-notes/xcode-27-release-notes>
- <https://developer.apple.com/videos/play/wwdc2026/8005/>

Il gate VoiceOver reale su iPhone e iPad resta `BLOCKED_EXTERNAL`. Gli audit
XCTest e le gerarchie AX sono verdi, ma non sostituiscono VoiceOver reale.

#### Cleanup

Il controller ha chiuso:

- VoiceOver macOS;
- MediFlow macOS;
- simulatori audit iPhone e iPad;
- sessione Playwright `mediflow-fallback`;
- Chromium con profilo temporaneo;
- server localhost sulla porta 3181;
- tab Chrome controllata dalla lane.

Next ha creato un drift di harness fuori scope:

- quattro righe automatiche in `tsconfig.json`;
- 1,4 GB di output non tracciato nella directory `private/`.

Il controller ha ripristinato `tsconfig.json`. La directory generata è stata
spostata nel Cestino:

`~/.Trash/mediflow-parity-generated-private-20260728-2302`

Il worktree conserva solo gli otto path previsti.

Verdetto prima del fresh verifier:

`PARTIAL / BLOCKED_EXTERNAL_MOBILE_VOICEOVER / HOLD_PROMOTION`.

Il blocker impedisce il commit locale previsto dal contratto. Nessun push, PR,
merge, tag o rilascio è autorizzato.

### 15.25 Fresh verifier e decision audit finale

Il fresh verifier `/root/release_gate_verifier` ha usato `gpt-5.6-sol` con
effort high. La verifica è stata read-only.

Freeze verificato:

- HEAD: `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- tree: `c46d739b026e509a3e1fae2348372a420c9a17aa`;
- otto path modificati;
- index vuoto;
- zero file untracked;
- diff SHA-256:
  `768f887b13debb6f0e395d948cbd4179ad9c0f0feaff68075f8abbef42d7da01`;
- `git diff --check`: PASS.

Il verifier ha validato:

- iPhone: 2/2 PASS, zero failure e zero skip;
- iPad: 7/7 PASS, zero failure e zero skip;
- macOS Cmd-R: PASS con focus nella sidebar e nel dettaglio;
- localhost: 78/78 PASS;
- assenza del probe VoiceOver temporaneo nel sorgente;
- cleanup dei processi, delle porte e dei simulatori.

Verdetto indipendente:

`PARTIAL / BLOCKED_EXTERNAL_MOBILE_VOICEOVER / HOLD_PROMOTION`.

Il loop identifier è chiuso. Il risultato non è
`BLOCKED_TAB_IDENTIFIER_IOS27`, perché il test atomico passa sul candidato
esatto.

Decision audit:

| Decisione | Evidenza | Assunzione | Generalità | Alternativa | Falsificatore | Verifica | Stato e rischio residuo |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Conservare il bridge identifier corrente | XCUITest iPhone 2/2 PASS | Il controllo UIKit pubblico resta il target della tab | bounded | Nuovo bridge o timing tweak | Identifier assente o duplicato in un nuovo XCUITest | Bundle `.xcresult` iPhone | `accepted`; nessun altro retry |
| Non riaprire Fable | Audit Fable `NO_WRITER_REQUIRED`; nessun difetto estetico nuovo | I delta finali sono semantici e di test | bounded | Nuovo packet estetico | Nuovo difetto visivo P0/P1 provato | Audit Fable e runtime corrente | `accepted`; nessun writer visivo |
| Fermare il probe VoiceOver dopo il primo tentativo pubblico | `enable()` non terminale e ciclo `com.apple.accessibility.heard` | Il difetto appartiene alla beta Xcode 27 | workaround | Bridge privato o modifica dei default del simulatore | API pubblica terminale su una seed successiva | Header Xcode, log runtime e fonti Apple | `accepted`; limite esterno |
| Rimuovere il probe VoiceOver temporaneo | Il probe blocca la suite e non produce prova positiva | Un test non terminale non deve entrare nel candidato | general | Conservare un test rosso | File XCUITest diverso dall'hash dei bundle verdi | SHA-256 del file e fresh verifier | `corrected`; nessun test rosso residuo |
| Ripristinare il drift Next | `tsconfig.json` e `private/` sono stati creati dal server della lane | Nessun altro writer possedeva tali output | bounded | Conservare output generati | Path extra nello status finale | `git status`, processi e porte | `corrected`; output nel Cestino |
| Non creare il commit locale | VoiceOver reale mobile non è provato | Il contratto richiede tutti i gate terminali verdi | general | Commit del candidato parziale | VoiceOver iPhone e iPad terminale su hardware o runtime idoneo | Fresh verifier Sol high | `open`; blocca promotion |

Confine non verificato: VoiceOver reale su iPhone e iPad.

Promotion: `not ready`.

Motivo: Apple Xcode 27 beta 2 non completa l'abilitazione VoiceOver nel
simulatore. Gli altri gate non possono sostituire questa prova.

Next action minima:

1. usare una seed Xcode 27 che risolva `173507341`, oppure due device fisici;
2. eseguire il solo test VoiceOver pubblico su iPhone e iPad;
3. creare il commit locale solo dopo due risultati terminali PASS.

### 15.26 Authority root: deroga VoiceOver e packet release

Checkpoint: `2026-07-28T23:26+02:00`.

L'authority root ha accettato il limite VoiceOver mobile come eccezione esterna
per la sola candidata sorgente GitHub 0.8. La decisione non trasforma il gate in
PASS.

Claim vietati:

- VoiceOver verificato su iPhone o iPad;
- piena conformità accessibilità;
- prova su device fisico;
- App Store readiness, certificazione o conformance.

Ogni altro gate rosso o indeterminato resta bloccante.

Il packet release possiede in modo sequenziale:

- gli otto path parity congelati;
- `README.md`, `CHANGELOG.md`, `PRODUCT.md` e `DESIGN.md`;
- `docs/README.md`, `docs/markdown-index.md`,
  `docs/known-limitations.md`, `docs/STATE_OF_THE_SYSTEM.md`,
  `docs/ROADMAP.md`, `docs/NATIVE.md`, `docs/parity-matrix.md` e
  `docs/apple-parity-matrix.json`;
- i metadati Apple in `project.yml`, nei due `Info.plist` e nel progetto Xcode;
- `screenshots/0.8/` e il relativo manifest.

Il packet Open Minis ADR 0089 resta separato. Nessun file ADR, provider runtime
o implementazione post-0.8 entra nella release.

### 15.27 Fable release narrative e media review

La sessione Claude Desktop è nuova e dedicata:

- sessione UI: `local_2d56d6e6-b176-4ec4-8d56-9213bf3ecb3f`;
- sessione CLI collegata: `de70a016-22ce-418a-acf8-9cc62051b59b`;
- titolo: `CoS parity 0.8 public release review`;
- root: worktree release candidate esatto;
- modello: `claude-fable-5`;
- effort: `high`;
- permission mode: `bypassPermissions`;
- UltraCode: `false`;
- meter visibile: `Weekly · Fable Resets Tue 9:59 AM 29%`, sezione Usage,
  interpretato come 29% usato al 2026-07-28;
- prompt SHA-256:
  `b94244aace9789c8cadaee6412516c52f4fe4424e7597a38137c87920724c579`;
- composer della sessione riuscita: vuoto prima dell'inserimento;
- inserimenti: 1;
- invii: 1;
- messaggi utente nel transcript: 1.

Un tentativo strutturale precedente si era fermato nel pre-composer perché il
branch era già aperto nel worktree corretto. Il composer è stato svuotato e
nessun messaggio è stato inviato. Non costituisce una seconda sessione
contaminata.

Fable ha lavorato in sola lettura. Ha verificato branch, HEAD, tree, otto path
dirty, index vuoto, zero untracked e fixture demo offline sintetica.

Verdetto:

`FABLE_RELEASE_MEDIA_READY / NO_UI_WRITER_REQUIRED`.

Finding:

- i difetti UI REQUIRED_0_8 del precedente audit sono chiusi;
- il README e i media nativi sono il solo residuo REQUIRED_0_8;
- i residui estetici P2/P3 restano POST_0_8;
- `PRODUCT.md` e `DESIGN.md` definiscono governance e direzione, senza
  introdurre funzioni;
- la fixture demo popola la workspace pazienti; Agenda, Diario e Analytics
  mostrano correttamente lo stato di connessione. Nessuna immagine può simulare
  una Agenda popolata.

Shot list approvata:

1. iPhone, worklist;
2. iPhone, scheda paziente sintetico;
3. iPhone, terapie e osservazioni;
4. iPad landscape, workspace list-detail;
5. iPad landscape, scheda complessa;
6. iPad portrait, scala di valutazione senza salvataggio.

Il giudizio estetico non richiede un altro writer. Codex conserva authority,
capture, verifica, commit e promotion.

### 15.28 Media packet sintetico

Checkpoint: `2026-07-28T23:41+02:00`.

Codex ha acquisito sei immagini dalla build Debug reale del candidato:

1. iPhone portrait, worklist;
2. iPhone portrait, scheda clinica;
3. iPhone portrait, terapie;
4. iPad landscape, workspace list-detail in stato vuoto;
5. iPad landscape, scheda clinica a `accessibility1`;
6. iPad portrait, scala ADL a `accessibility1`.

La sola fonte dati è `MEDIFLOW_APPLE_DEMO=1`. La modalità demo:

- non legge Keychain, rete o database;
- non usa credenziali;
- usa dati deterministici dichiarati sintetici;
- non salva le risposte della scala.

Per le due immagini iPad con dettaglio, gli override Debug già presenti
`MEDIFLOW_APPLE_UITEST_OPEN_PATIENT_INDEX`,
`MEDIFLOW_APPLE_UITEST_FOCUS_DETAIL` e
`MEDIFLOW_APPLE_UITEST_PRESENT_SCALE` hanno aperto la scena richiesta. Il
controller non ha modificato il codice per la fotografia.

Gli originali PNG lossless sono in `screenshots/0.8/original/`. Le copie per
GitHub sono state ridimensionate con `sips -Z 1400`. L'orientamento dei tre
frame iPad è stato normalizzato con rotazioni lossless di 90 o 180 gradi. Non
sono stati aggiunti mockup, cornici, compositing o interfacce generate.

Il manifest `screenshots/0.8/manifest.json` registra:

- branch, HEAD e tree di base;
- Xcode `27A5194q` e runtime iOS `24A5355p`;
- fixture, scena, orientamento e Dynamic Type;
- dimensioni, testo alternativo e SHA-256 per originale e copia;
- confine privacy: zero database reale, credenziali reali, PHI o PII.

Controllo visivo:

- status bar coerente a `09:41`;
- nessuna notifica, chrome di debug, cursore o banner della fixture;
- label, valori e stati coerenti con la scena;
- dati visibili provenienti soltanto dal dataset demo.

VoiceOver non è stato avviato durante la cattura.

### 15.29 Verifica finale web e correzione bounded del test

Checkpoint: `2026-07-29T00:19+02:00`.

Il primo run completo dei 23 file Playwright ha eseguito 78 test. Ha prodotto
77 PASS e un failure in
`e2e/document-stack-accessibility.spec.ts`.

Lo screenshot del failure mostrava contemporaneamente:

- i dati sintetici del paziente già caricati;
- la sezione `Archivio documenti ed evidenze` ancora chiusa;
- nessun difetto visivo o funzionale della tile Evidence Stack.

La causa osservata era una race del test durante l'idratazione. Il helper
leggeva `aria-expanded` prima che l'attributo avesse un valore booleano e
continuava senza aprire la sezione.

La correzione è limitata al test:

1. attende `aria-expanded=true|false`;
2. attiva il controllo quando il valore non è `true`;
3. verifica lo stato terminale `aria-expanded=true`.

Nessun componente, testo clinico, dato o comportamento di prodotto è stato
modificato per rendere verde il test.

Verifiche:

- run focalizzato Evidence Stack: 2/2 PASS;
- run completo dei 23 file: 78/78 PASS, zero retry;
- durata run completo: 7,5 minuti;
- Node: `v24.18.0`;
- log terminale:
  `/private/tmp/mediflow-08-final-all-e2e-green.log`.

### 15.30 Gate Apple sul candidato finale

Toolchain:

- Xcode 27 beta, build `27A5194q`;
- runtime iOS 27, build `24A5355p`;
- fixture esclusivamente sintetiche.

iPhone:

- risultato `.xcresult`: `Passed`;
- 36 test scoperti;
- 32 PASS;
- 4 skip specifici per capacità non applicabili alla configurazione iPhone;
- zero failure;
- bundle:
  `/tmp/mediflow-08-final-iphone-exact.xcresult`.

iPad:

- risultato `.xcresult`: `Passed`;
- 36 test scoperti;
- 30 PASS;
- 6 skip specifici per capacità compact non applicabili alla configurazione
  iPad;
- zero failure;
- bundle:
  `/tmp/mediflow-08-final-ipad-exact.xcresult`.

La build macOS del tree corrente è `BUILD SUCCEEDED`. Il probe AX reale sulla
stessa app ha verificato:

- apertura delle sezioni cliniche;
- ruolo nativo della lista pazienti;
- selezione del primo e del secondo paziente sintetico;
- binding riga, intestazione e contenuto patient-bound;
- stabilità del binding dopo scroll;
- identifier dei moduli e dei controlli principali.

Il probe ha stampato `Native click-map probe passed`. Il primo launcher aveva
interrogato `NSRunningApplication` prima che il processo fosse registrato. La
ripetizione diagnostica ha atteso la registrazione dello stesso processo prima
del probe. Non sono state cambiate app o fixture. Il successivo exit non-zero
del wrapper diagnostico era causato dal nome shell riservato `status`, dopo il
verdetto terminale del probe. Il PID MediFlow creato dalla lane è stato chiuso.

Evidenze:

- build macOS:
  `/private/tmp/mediflow-08-final-macos-exact-build.log`;
- probe macOS:
  `/private/tmp/mediflow-08-final-macos-probe-manual.log`;
- nessun processo VoiceOver avviato;
- nessun processo MediFlow posseduto lasciato attivo;
- simulatori audit iPhone e iPad: `Shutdown`;
- nessun listener posseduto sulle porte 3100, 3180-3183 e 9333.

Il limite VoiceOver mobile resta `accepted external limitation`. Non è un PASS.
La deroga vale solo per la candidata sorgente GitHub 0.8 e non sostiene claim
App Store, certificazioni, piena conformità accessibilità o test su device
fisico.

### 15.31 Validazione release prima del commit

Il controller ha eseguito sul tree candidato:

- `npm run lint`: PASS;
- `npm run typecheck`: PASS;
- `npm run build`: PASS, inclusa uscita standalone;
- `npm run check:never-regress`: PASS;
- `npm run check:claims`: PASS, 748 file e zero claim ad alto rischio;
- `npm run check:apple-wide-qa`: PASS, 24 controlli;
- `npm run check:lume-tokens`: PASS, 42 coppie e zero sotto soglia;
- `npm run check:motion-budget`: PASS;
- `npm run check:openapi:drift`: PASS;
- `npm run check:schema-drift`: PASS, 21 tabelle e 22 indici;
- `npm run test:unit`: PASS, 896 test;
- `npm run test:native`: PASS, 559 test;
- Playwright completo: PASS, 78/78;
- manifest media: JSON valido e 12/12 hash SHA-256 esatti.

I metadati sono allineati:

- `package.json`: `0.8.0`;
- `project.yml`: `MARKETING_VERSION=0.8.0`;
- Info.plist iOS e macOS: `CFBundleShortVersionString=0.8.0`;
- build number: `1`.

Decision audit:

| Decisione | Evidenza | Alternativa | Falsificatore | Stato |
| --- | --- | --- | --- | --- |
| Conservare la UI approvata da Fable | `NO_UI_WRITER_REQUIRED` e gate runtime verdi | Nuovo redesign | P0/P1 estetico provato | `accepted` |
| Correggere solo l'attesa del test Evidence Stack | Failure mostra sezione chiusa durante idratazione; 2/2 e 78/78 verdi | Cambiare il componente | Failure ripetibile dopo stato booleano terminale | `corrected` |
| Allineare i metadati Apple a 0.8.0 | Package e documenti dichiarano 0.8.0 | Conservare versioni 0.1.0/1.0 divergenti | Bundle generato con versione diversa | `accepted` |
| Accettare il limite VoiceOver mobile senza promuoverlo a PASS | API pubblica Xcode 27 beta non terminale | Claim di conformità non provato | Test reale terminale su runtime idoneo | `accepted external limitation` |
| Usare solo la fixture demo offline per i media | Manifest, codice fixture e immagini verificati | Seed con identificativi realistici | Dato non sintetico in immagine o manifest | `accepted` |
| Tenere Intelligence Fabric e Open Minis fuori dalla 0.8 | Documenti li classificano post-0.8; nessun path provider nel packet | Integrare una nuova funzione | Diff runtime/provider nel commit | `accepted` |
| Autorizzare solo un push non forzato dopo verifica indipendente | Authority root e policy repository | Merge, tag o release | Remote drift o verifier non terminale | `accepted` |

Confine aperto e dichiarato:

- VoiceOver reale su iPhone e iPad non è stato provato.

Il confine non blocca la candidata sorgente per decisione esplicita
dell'authority root. Blocca qualsiasi claim più ampio.

Promotion rule:

1. safe commit locale dei soli path verificati;
2. fresh verifier indipendente sul commit esatto;
3. push non forzato solo dopo `APPROVE_POST_COMMIT`;
4. verifica che il ref remoto punti al commit esatto;
5. nessun merge, tag, GitHub Release, PR o chiusura issue.

### 15.32 Fresh verifier post-commit e correzioni

Il safe commit iniziale era:

- commit: `9393a7ef3a3050f4e5700cc5f86f159888089464`;
- tree: `35982cd6fac85f234d914ebe3e1ac4da58e3eb43`;
- parent: `2355a46a4dde63b1956a2298d99ef0b5c4208222`;
- 36 path, 2395 inserimenti e 122 rimozioni;
- worktree pulito e index vuoto.

Il fresh verifier indipendente `gpt-5.6-sol/high` ha emesso
`BLOCKED_POST_COMMIT`. Ha trovato tre falsificatori:

1. `CHANGELOG.md` dichiarava 6 rilievi `npm audit`, mentre l'audit live ne
   registrava 21 nel tooling di sviluppo;
2. `ipados-detail.png` e l'originale erano capovolti;
3. l'indice Markdown dichiarato completo ometteva 15 file tracciati.

Il controller non ha eseguito il push.

Correzioni:

- changelog e limitazioni note registrano 21 rilievi dev-tooling:
  1 low, 5 moderate, 15 high e zero critical;
- `npm audit --omit=dev` resta PASS con zero vulnerabilità production;
- l'immagine iPad detail, originale e ottimizzata, è stata ruotata di 180
  gradi; il manifest registra la normalizzazione totale di 270 gradi e i nuovi
  hash;
- i 15 Markdown mancanti sono stati aggiunti all'indice.

La lane read-only `gpt-5.6-terra/high` ha valutato una remediation delle
dipendenze. Un candidato temporaneo portava l'audit completo a zero, ma:

- forzava `esbuild` e `brace-expansion` fuori dai range dei consumer;
- introduceva ESLint 10 con peer transitivi non allineati;
- modificava 172 nodi del lockfile, inclusi due pacchetti browser-data di
  produzione;
- falliva `npm ci --dry-run --ignore-scripts --strict-peer-deps`.

Verdetto della lane: `REJECT_OVERRIDE`.

Decisione:

- non assorbire una toolchain incompatibile nella candidata 0.8;
- mantenere il rischio dev-tooling aperto e pubblico;
- richiedere un packet dipendenze separato, senza override fuori range e con
  regressione completa;
- ripetere commit sicuro e fresh verifier sul nuovo hash prima del push.

### 15.33 Chiusura della worklist compatta

Run ID: `parity-0.8-compact-closure-20260729`.

Modalità: `manager-goal`, con Codex come controller-of-record tecnico.

Root:
`/Users/leonardopegollo/.codex/worktrees/mediflow-0.8-release-candidate-local/medical-record-app`.

Branch: `codex/mediflow-0.8-release-candidate-local`.

HEAD di base: `c0d38b946e7873c831691f499c61c9fe5c5e62ff`.

#### Lane e autorità

- CoS parity 0.8: `gpt-5.6-sol`, effort high;
- Fable UI: sessione isolata
  `local_4f85ddab-9f02-40d9-97c7-4cd060b26c27`, Fable 5, effort High,
  Bypass permissions, UltraCode disattivato;
- prompt Fable atomico SHA-256:
  `a53a8a0b84f25f8992fc2100396d93eb73675670832095957ab96b3a5c4892b8`;
- verdetto Fable: `APPROVE_UI_PACKET`;
- verifier indipendente: `gpt-5.6-sol`, effort high, read-only.

La lane UI non ha usato né aggiornato sessioni Open Minis. Le tre sessioni
Open Minis con stato `Needs input` restano post-0.8 e non bloccano questa
release.

#### Correzione

Il packet compatto modifica soltanto CSS Lume e test Playwright. Non modifica
DOM, identifier, azioni, servizi, testo clinico, dati, backend o token
condivisi.

La correzione:

- riduce il rail e gli inset a 320 e 390 px;
- mantiene i target interattivi ad almeno 44 px;
- conserva il focus visibile da tastiera;
- rimuove un livello decorativo annidato dal frame compatto;
- rende la gerarchia e le righe paziente leggibili senza overflow;
- aggiunge verifiche a 600 e 701 px per i confini responsive;
- mantiene lo snapshot ARIA invariato tra 320 e 481 px.

Le viste localhost a dimensione telefono o tablet restano solo evidenza di
test. La galleria pubblica usa catture native iPhone e iPad. Le catture web
pubbliche restano desktop.

#### Errore rilevato e correzione del contratto

Il primo run completo dopo il packet ha prodotto 78 PASS e quattro failure.
I failure riguardavano `e2e/lume-frame.spec.ts`: il test richiedeva ancora il
fondo `--lume-surface-field` sul pannello esterno a 320 e 390 px.

Il comportamento runtime era intenzionale e già approvato da Fable: sotto
481 px il pannello esterno è trasparente e senza bordo, mentre la superficie
focale conserva il raggruppamento e il bordo. Il test è stato aggiornato per
verificare in modo esplicito entrambi i contratti:

- fino a 480 px: pannello trasparente e bordo di 0 px;
- oltre 480 px: superficie field e bordo di 1 px.

Le verifiche di token, focus, overflow, gerarchia e superficie focale non sono
state rimosse o indebolite.

#### Evidenze runtime

Web, Node `v24.18.0`:

- frame e worklist focalizzati: 21/21 PASS;
- suite completa: 82/82 PASS, zero failure e zero skip;
- durata suite completa: 7,8 minuti;
- database sintetico isolato:
  `/tmp/mediflow-e2e-final.1sLfNy`.

Apple, Xcode 27 beta build `27A5194q`, runtime `24A5355p`:

- iPhone 17 Pro: tab identifier 1/1 PASS;
- bundle:
  `/tmp/mediflow-ios27-iphone-green.tMBLGp/iphone.xcresult`;
- iPad Pro 13-inch (M5): matrice parity 7/7 PASS;
- bundle:
  `/tmp/mediflow-ios27-ipad-green.296li3/ipad.xcresult`;
- macOS arm64: build Debug PASS;
- derived data:
  `/tmp/mediflow-xcode27-macos.opubLF/DerivedData`;
- simulatori audit: `Shutdown`;
- VoiceOver mobile non è stato avviato.

Il primo mount in sola lettura ha compilato i target iOS 27, ma ha impedito
l'installazione nel simulatore. Il controller ha sganciato le attach stale e
ha rimontato lo stesso sparsebundle in modalità scrivibile. Il nuovo test ha
superato il punto di failure precedente. Nessuna riparazione del filesystem è
stata eseguita.

#### Decision audit

| Decisione | Evidenza | Alternativa | Falsificatore | Stato |
| --- | --- | --- | --- | --- |
| Conservare il frame esterno trasparente sotto 481 px | Approvazione Fable e 21/21 focalizzati | Ripristinare un bordo decorativo | Perdita di raggruppamento, focus o contrasto | `accepted` |
| Aggiornare il test del frame | Il test storico contraddiceva il contratto compatto approvato | Cambiare la produzione per il test | Failure su superficie focale, token o overflow | `corrected` |
| Non pubblicare viste web compresse come media mobile | Manifest e dimensioni provano catture native separate | Usare proxy responsive nella galleria | Asset iPhone/iPad proveniente da Chromium | `accepted` |
| Rimontare il volume simulatori scrivibile | Il failure era `Read-only file system`; la ripetizione supera installazione e test | Conservare il mount read-only | Nuovo failure di scrittura sul device set | `corrected` |
| Conservare la deroga VoiceOver mobile | Authority root e limitazione pubblica | Dichiarare un PASS non provato | Test VoiceOver reale terminale | `accepted external limitation` |

Confine aperto:

- VoiceOver reale su iPhone e iPad resta non provato;
- la deroga non sostiene claim App Store, certificazione o piena conformità.

Promotion rule corrente:

1. fresh verifier indipendente sul diff esatto;
2. safe commit locale dei soli path verificati;
3. verifica post-commit sul commit esatto;
4. push non forzato del branch;
5. pull request e merge solo con controlli remoti verdi e assenza di drift;
6. tag `v0.8.0` e GitHub Release solo sul commit `main` verificato;
7. stop su test rosso, secret, remote drift o necessità di force push.

### 15.34 Fresh verifier del packet compatto

Il verifier indipendente `gpt-5.6-sol/high` ha controllato il diff con:

- branch `codex/mediflow-0.8-release-candidate-local`;
- HEAD `c0d38b946e7873c831691f499c61c9fe5c5e62ff`;
- nove path dirty attesi;
- index vuoto e zero file untracked;
- diff SHA-256
  `2c2582eb99e9cbfa8d72f462677e7e40b0bd903a529c2b2af4d2efd4f3900553`.

Controlli indipendenti:

- `git diff --check`: PASS;
- ESLint mirato sui due file Playwright: PASS;
- TypeScript `--noEmit`: PASS;
- parse JSON della matrice Apple: PASS;
- parse PostCSS dei tre file CSS: PASS;
- focus visibile, breakpoint, chiusura 320 px, confine media e privacy:
  nessun finding bloccante.

Verdetto: `APPROVE_COMMIT_UI_FIX`.

### 15.35 Correzione del confine CI e nuova prova Xcode 27

Il commit `68b8b971993bedfd7ded1c72eec415836037194b` è stato
pubblicato sul branch della candidata e ha aperto la pull request 148. Sei
controlli remoti sono passati. Il controllo E2E ha trovato quattro failure
ripetibili su Ubuntu:

- a 320 px, titolo e conteggio della worklist occupavano righe diverse;
- a 768 px, il rail superiore tagliava l'ultima destinazione;
- i due failure erano presenti nei registri giorno e grafite.

Gli artefatti del run GitHub `30429611097` hanno confermato che si trattava di
difetti di geometria dipendenti dalle metriche del browser e dei font. Il
controller non ha modificato né ridotto le asserzioni.

Correzione bounded:

- la testata compatta usa una griglia esplicita con titolo e conteggio sulla
  stessa riga;
- l'azione `Nuova scheda` occupa la riga successiva e conserva il target
  interattivo di almeno 44 px;
- il breakpoint del rail include 768 px, quindi tutte le destinazioni restano
  nel flusso e non richiedono uno scorrimento orizzontale nascosto;
- contenuto, identifier, azioni, servizi, stato e token restano invariati.

Le viste localhost a 320, 390 e 768 px sono solo prove tecniche di reflow,
accessibilità e clipping. Non rappresentano l'app iPhone o iPad e non fanno
parte della galleria pubblica. Gli asset pubblici mobile restano catture native
della build Xcode.

Evidenze web sul diff corretto, Node `v24.18.0`:

- frame e worklist focalizzati: 21/21 PASS;
- suite completa, un solo worker: 82/82 PASS in 7,7 minuti;
- database sintetico:
  `/tmp/mediflow-e2e-full-remediation.eUIj4N`;
- lint, typecheck, claims guard e never-regress: PASS;
- claims guard: 748 file, zero claim ad alto rischio;
- token Lume: 30/30 PASS;
- motion budget: 9/9 PASS;
- build di produzione: PASS, 104 pagine statiche generate.

Il volume Xcode 27 beta è montato e utilizzabile:

- Xcode `27.0`, build `27A5194q`;
- runtime iOS 27 e simulatori audit eleggibili;
- iPhone: 8/8 PASS;
- iPad: 8/8 PASS;
- prove incluse: AX5, audit AX, navigazione delle superfici, identifier delle
  tab, scorrimento sotto la tab bar, rotazione iPad e conservazione del
  dettaglio;
- bundle iPhone:
  `/tmp/mediflow-xcode27-rerun.3pbRuL/iphone.xcresult`;
- bundle iPad:
  `/tmp/mediflow-xcode27-rerun.3pbRuL/ipad.xcresult`;
- i due simulatori sono tornati nello stato `Shutdown`;
- VoiceOver udibile non è stato avviato.

Decision audit:

| Decisione | Evidenza | Alternativa | Falsificatore | Stato |
| --- | --- | --- | --- | --- |
| Correggere la produzione e non indebolire i test | Screenshot CI mostrano composizione e clipping reali | Aumentare le tolleranze | Nuovo overflow o perdita di gerarchia | `accepted` |
| Includere 768 px nel layout rail a capo | L'ultima voce era fuori dal rail | Conservare lo scorrimento orizzontale | Destinazione non visibile o non raggiungibile | `accepted` |
| Trattare i proxy stretti come sola evidenza tecnica | Decisione utente e media manifest nativo | Pubblicarli come viste mobile | Asset mobile acquisito da Chromium | `accepted` |
| Ripetere la matrice nativa su Xcode 27 | 8/8 iPhone e 8/8 iPad sullo snapshot corrente | Affidarsi ai soli run precedenti | `.xcresult` rosso o runtime non eleggibile | `accepted` |

Promotion rule:

1. fresh verifier indipendente sul nuovo diff;
2. commit locale aggiuntivo senza riscrivere la storia remota;
3. push fast-forward sul branch della pull request 148;
4. tutti i controlli remoti devono tornare verdi;
5. merge, tag e GitHub Release solo sul commit `main` verificato;
6. stop su test rosso, secret, drift remoto o necessità di force push.

### 15.36 Packet post-release: media macOS, topologia e uso assistito

Run ID: `COS-PARITY-08-MACOS-MEDIA-TOKENS-20260729`
Modalità: `manager-goal / single writer / independent review`
Root: worktree locale dedicato; percorso personale omesso dal documento pubblico
Branch: `codex/mediflow-0.8-macos-media-tokens`
Base: `0cef4f4ae41c2c04efd5e1343cf3049215f36c9e`
Tree base: `6d1e774348241d8ad28327308187b68a6fbf26bb`

Obiettivo bounded:

- sostituire lo screenshot macOS legacy con una cattura reale dell'app corrente;
- escludere dalla galleria le viste localhost ristrette a dimensioni telefono
  o tablet;
- descrivere il rapporto tra Mac home-base, localhost, iPhone e iPad;
- presentare Intelligence Fabric soltanto come direzione post-0.8;
- rigenerare il dashboard pubblico dei token tramite CodexBar.

Confini:

- nessun file o runtime Open Minis;
- nessun dato reale, credenziale, prompt o log privato;
- nessun mockup o compositing dell'interfaccia;
- nessun claim App Store o di conformità completa;
- nessun push, PR o merge prima del verificatore terminale.

Evidenze disponibili:

- Xcode beta `27.0 (27A5194q)` sul volume `XcodeVision`;
- build reale `MediFlowMacApp`, configurazione Debug, destinazione macOS:
  `BUILD SUCCEEDED`;
- modalità `MEDIFLOW_APPLE_DEMO=1`: fixture deterministiche, nessun Keychain,
  database, credenziale o accesso di rete;
- review estetica e informativa read-only con `claude-opus-5`, effort Medium:
  home-base nativo come immagine principale, localhost desktop separato,
  topologia corrente separata dall'anteprima Intelligence Fabric;
- CodexBar `0.45.2`, finestra richiesta di 365 giorni, periodo disponibile
  `2026-02-01`–`2026-07-29`;
- snapshot terminale CodexBar: `34.887.730.402` token; Codex
  `25.026.076.888`; Claude Code `9.861.653.514`; cache letta
  `32.999.582.699` (`94,6%`);
- parser CodexBar corretto in fail-closed per campi obbligatori, date ISO,
  giorni duplicati, somme non sicure e cache incoerente;
- prova con fixture temporanea: input incompleto rifiutato senza modificare gli
  output; giorni in ordine inverso normalizzati al periodo minimo/massimo;
- claims guard e never-regress: PASS prima della chiusura del packet.

Review indipendente:

- il primo pass ha identificato due P1: artefatti non ancora rigenerati e
  validazione troppo permissiva dell'output CodexBar;
- ha identificato tre P2: lookup non portabile, invocazione non documentata e
  scrittura non coordinata dei due artefatti;
- tutte le correzioni sono incluse nel candidato terminale;
- fresh verifier Sol High: `APPROVE_PACKET`;
- scope e index: PASS;
- test Swift documenti: 8/8 PASS;
- claim, never-regress, JSON, sintassi Node e `git diff --check`: PASS;
- il primo verificatore post-commit ha emesso `HOLD_POST_COMMIT` per un link
  Lume rimasto sullo screenshot legacy eliminato;
- il link ora usa `screenshots/0.8/macos-clinical-workspace.png` e descrive le
  fixture sintetiche;
- serve un nuovo verdetto post-commit sul commit correttivo esatto.

Blocco risolto:

- l'utente ha sbloccato la sessione grafica macOS;
- la build nativa ha aperto 20 pazienti sintetici con
  `MEDIFLOW_APPLE_DEMO=1`;
- la modalità demo non riconosceva il proprio dataset come fixture UI;
- il caricamento automatico dei documenti mostrava quindi un falso errore di
  sessione paired;
- il predicato condiviso ora include il dataset demo;
- il ramo documenti pubblica un insieme vuoto deterministico e il messaggio
  `Nessun documento caricato per questo paziente.`;
- build macOS dopo la correzione: `BUILD SUCCEEDED`;
- verifica runtime: nessun errore paired, nessuna rete e nessuna credenziale;
- cattura nativa lossless:
  `screenshots/0.8/original/macos-clinical-workspace.png`, 3200×1800,
  SHA-256 `edaaa0abe3bfff378b612c9b7a502d0c149c92dacb0dc830332193c5d47d9a12`;
- copia GitHub:
  `screenshots/0.8/macos-clinical-workspace.png`, 1600×900,
  SHA-256 `80bb20fdfdaed2c832e533a27acc27de7b1101c7095de820d1d1529c4954098d`;
- lo screenshot macOS legacy è stato rimosso dopo la verifica del sostituto.

Promotion rule:

1. rigenerazione finale CodexBar sullo snapshot da committare;
2. verifica di manifest, asset, link, claim, build e test;
3. fresh verifier sul diff e sugli asset esatti;
4. safe commit locale;
5. verifica post-commit;
6. push non forzato del solo branch verificato;
7. stato corrente:
   `APPROVE_PACKET / READY_FOR_SAFE_COMMIT / HOLD_PROMOTION`.

Decision audit:

| Decisione | Evidenza | Alternativa esclusa | Verifica | Stato e rischio residuo |
|---|---|---|---|---|
| Trattare il dataset demo come fixture UI | La demo caricava i dati ma seguiva il ramo paired per i documenti | Ritoccare lo screenshot o usare credenziali reali | Build, runtime e 8/8 test documenti | Accettata; modifica solo `DEBUG` |
| Pubblicare la finestra Mac nativa | Cattura PNG 3200×1800 con dati sintetici | Conservare lo screenshot legacy o una vista web mobile | Ispezione visiva, dimensioni e SHA-256 | Accettata; nessun dato reale |
| Mostrare localhost solo come desktop | Le viste web strette restano evidenze responsive | Usarle come sostituti di iPhone o iPad | Galleria e link README | Accettata; nessuna prova rimossa |
| Separare 0.8 e Intelligence Fabric | Il contratto 0.8 non consegna il routing completo | Presentare la roadmap come funzione live | Claims guard e review dei testi | Accettata; direzione futura esplicita |
| Attribuire i token a Codex e Claude Code | CodexBar aggrega in base al processo che registra l'uso | Attribuire ogni token al fornitore del modello | Riconciliazione CodexBar e grafico | Accettata; il dato non misura costo o qualità |

Unverified edges: VoiceOver reale mobile resta l'eccezione esterna già
documentata per la release sorgente 0.8.

Promotion: pronta per commit locale. Il push richiede la verifica post-commit
del commit esatto e l'assenza di drift remoto.
